# 网约车物语 — 游戏设计文档

最后更新:V15.16(2026-05-10)

> V15.16 投资人定期 review 机制(惩罚 + 目标驱动双轨):
>
> 现有的"投资人压力"事件只在资金负时触发,稳健玩家(慢扩张、2-3 辆车一直积攒)永远不会触发,失去经营张力。新增"投资人定期 review"机制,按绝对游戏时间(day 90/180/270/360,对应 Q1/半年/Q3/年终)主动评估玩家 KPI。前期(Q1、半年)是惩罚导向 — 不达标就警告/罚款;后期(Q3、年终)是目标驱动 — 达标就解锁更高愿景(冲 Tier 4-5)。Q3 是关键分岔点:连续 3 次不达标触发撤资事件,扣大笔现金,通过现有"资金 < 0 持续 5 天"破产逻辑进入新结局「被踢出局」。死亡判定保持单一(现金为负持续 5 天),所有新机制压力通过"扣现金"传导。详见第七章「投资人定期 review」小节。

> V15.15 链式事件玩家文案清理:
>
> 链式事件正式弹窗移除“链式事件”“借钱与家庭压力线”等系统/策划语言。玩家界面只展示 NPC 名字、事件标题、事件描述和可选择行动;chain、unlockMission、cooldown、事件线定位等保留在预览工具和配置文档中。

> V15.14 链式事件对话框返工:
>
> 修正 V15.13 中人物与弹窗关系错乱的问题。NPC 素材改为更接近经营游戏的 Q 版像素站绘,弹窗结构改为“左侧角色舞台 + 右侧文字牌”:人物站在弹窗左侧并探出上沿,文字牌从人物右侧开始,边框不再穿过人物。

> V15.13 链式事件立绘升级:
>
> 链式事件 NPC 素材从 SVG 占位替换为 AI 生成的透明 PNG 立绘。弹窗布局改为人物立绘 + 异形对话卡片,更接近早期经营/RPG 游戏的 NPC 对话表现。

> V15.12 链式事件 NPC 立绘:
>
> 随机事件中的 4 条真分支链式事件增加 NPC 画像定位和弹窗立绘。老张借钱线、平台抽成线、竞品挖人线、小李事故线分别对应老张、平台经理、滴答猎头、小李 4 个本地 NPC 素材。EventModal 会识别这些链式事件,改用左上 NPC 立绘 + 异形对话卡片布局,和普通随机事件形成明确区别,强化“这是一段连续人物剧情”的心智。

> V15.11 多笔债务与债务危机:
>
> 贷款从单一 `debtAmount/debtDueDay` 改为 `debts` 明细数组。扩张贷款、高利贷、事件贷款分别记录来源、本金、应还金额和到期日;顶栏资金区只展示最近到期的一笔,鼠标悬停资金说明时展开全部未还债务。到期资金足够时只扣今日到期债务;资金不足时不再无提示直接破产,改为暂停并弹出“债务危机”:玩家可选择放弃经营进入破产结算,或债务重组。债务重组会把全部未还债务打包为一笔“重组债务”,总待还 +5%,新期限按所有未还债务剩余天数累加,最少 14 天、最多 60 天。旧存档的 `debtAmount/debtDueDay` 会自动迁移成一笔历史债务。

> V15.10 月合规支出文案改为百分比:
>
> 监管整改里的“强制启动月合规支出”不再展示“首月金额 → 稳定金额”的孤立数字,改为“每月按流水扣:首月 25% → 稳定后 10%”。玩家更容易理解这是按经营规模持续发生的合规成本,而不是一次性固定罚款。

> V15.9 平台自营成本预览修正:
>
> `platform_pressure` 的自营小程序成本是固定门槛 ¥180,000,不参与事件规模缩放。弹窗预览现在和引擎结算共用同一条 skipScale 规则,不会再把 ¥180,000 预览成百万级支出。该选项增加 `requireFunds: 180000`,资金不足时按钮禁用,引擎层也做兜底拦截。

> V15.8 暂停菜单视觉减重:
>
> ESC 暂停菜单保留像素厚边、硬阴影和暖黄纸面感,但降低黑色标题栏、全宽高饱和按钮、粗边框和大阴影带来的压迫感。标题栏改为浅色信息条,菜单宽度收窄,主命令按钮高度、边框、阴影和底色饱和度同步收敛;“开始新游戏”保留红色风险语义,但从大面积警告红改为浅红底 + 红色标题。

> V15.7 ESC 暂停菜单与自动继续存档:
>
> 游戏增加单槽自动存档 `wycwy-autosave-v1`:开始运营后按关键状态变化、页面隐藏、刷新/关闭前自动保存完整可继续状态;刷新页面会自动载入最近存档并保持暂停,避免玩家丢失进行中的一局。ESC 在无关键弹窗时会暂停并打开游戏式系统菜单:顶部标题栏、主命令、底部设置。菜单主命令顺序为继续游戏、开始新游戏、载入上一次存档;载入按钮直接展示上次存档的运营天数、累计金钱、司机数、车辆数和保存时间,不再额外展示右侧存档详情。运营记录不放在暂停菜单里。底部设置包含音效/CRT/新手指引/诊断导出。顶栏右侧收敛为一个“设置 ESC”入口,打开同一套系统菜单。开始新游戏会清空可继续存档,但保留运营记录。运营记录中的当前局文案改为“当前快照/自动记录”,避免和真正的“继续游戏”存档混淆。

> V15.6 运营记录增加进行中快照:
>
> 运营记录不再只依赖通关/破产后的完局历史。玩家开始运营后,系统会把当前局快照节流保存到 `localStorage` 的 `wycwy-current-run-v1`;通关或破产时仍会把最终记录写入 `wycwy-run-history-v1`,并清掉当前局快照。目标弹窗的“运营记录”tab 会优先展示“当前快照”,再展示最近 20 局历史。注意这仍是复盘记录,不是完整继续游玩的存档恢复。

> V15.5 恢复强角色命名:
>
> 司机姓名恢复为强识别的稀有度命名逻辑:SR 使用「金牌 / 王牌 / 专车」等前缀,SSR 使用「赛车名人 + 称号后缀」。背景字段不再尝试弱化为泛泛类型,而是改成和姓名气质一致的角色说明,例如「平台金牌司机 / 管理型骨干 / 赛车系王牌 / 五星服务车神」。能力条去掉车技、服务前的小图标,保留「车技 / 服务」完整中文标签和颜色,避免图标无法解释含义时增加视觉负担。

> V15.4 司机姓名与背景分工:
>
> 抽卡、调度台和分配司机列表把背景降级为低权重角色标签或元信息,避免背景像第二个姓名一样抢主次。

> V15.3 完单入账反馈收敛:
>
> 司机完单入账音效从偏“任务完成”的双音提示改为本地 `assets/audio/coin-pickup.wav` 金币素材音,加载失败时保留短促三连音兜底,避免和任务完成音效混淆。8× / 4× 下按倍速自动降低音量并增加播放冷却,防止多车组同时完单时金币声重叠刺耳。地图 `+¥金额` 飘字同步缩小字号、描边和上浮幅度,颜色改用金币黄,并在数字右侧增加 SVG 像素金币与 3 颗短促粒子,让收入反馈保留存在感但不再遮挡城市地图。

> V15.3 UI 小图标素材化:
>
> 新增 `assets/pixel/generated-ui-icons-source.png` 生图素材源图,并由 `scripts/extract-generated-ui-icons.py` 裁切出 `assets/pixel/icons/` 下的 PNG 位图图标,覆盖 4 类订单、2 项司机能力、3 种招募券和任务/奖励/警告/故事/通关反馈图标。`OrderIcon` / `StatIcon` 不再使用纯色圆点或色块,招募券卡片增加券面图标,`GameFeedbackCard` 默认展示本地反馈图标,让弹窗、任务和订单标签更接近完整像素游戏资产。

> V15.3 招募券说明重构:
>
> 招募券选择页不再向玩家展示 `N/R/SR/SSR` 内部枚举,统一改成「新手 / 熟手 / 骨干 / 王牌」四档司机心智。每张券卡片新增「单名候选概率」,直接展示该券抽出各档司机的概率;卡片结构改为图标、用途定位、券名、价格、说明、概率、按钮,让玩家先理解这张券适合什么阶段,再决定是否花钱抽。

> V15.3 订单标签图标简化:
>
> 订单类型标签不再使用复杂小 PNG,改为单字色块徽标:「惠 / 快 / 专 / 豪」。原因是订单标签空间只有 16px 左右,复杂像素图缩小后无法识别;单字徽标与右侧订单名称互相印证,更适合高频扫读。

> V15.2 真实游玩时长精准统计:
>
> 引擎层新增 `realTime` 计时状态,从玩家第一次开始运营开始记录真实世界耗时。导出的诊断 JSON 会包含 `realTime`:开始时间、最后更新时间、导出时间、总真实耗时、实际运行耗时、暂停/弹窗耗时、游戏内小时数和平均有效倍速。`actionHistory` 的每条结构化日志也会带 `realTimestamp` 和真实耗时快照,之后可以精确分析一局实际玩了多久,不再只能按游戏天数反推估算。

> V14.93 反向拆分 — 单文件 8455 行 → 薄壳 106 行 + src/ 模块化:
>
> 把 HTML 内嵌的 5365 行 CSS 和 3178 行 React 组件按章节/职责反向拆到 `src/styles/*.css`(11 文件)和 `src/app/*.jsx`(9 文件)。HTML 改为 106 行薄壳,只放 `<link>` 和 `<script>` 引用。**前提变化**:必须用 http:// 协议访问,不能再双击 file:// 直接打开(Babel fetch 跨域限制);本地用 `python3 -m http.server 8765` 跑测试。Babel 多 script 作用域 PoC 验证共享全局,无需 window.X 显式导出。删除 V9 时代过期的 `src/app.jsx`(2008 行)/`src/app/`/`src/styles/` 全部副本和 `scripts/build-html.mjs`(守护断言已永久阻止重建)。Playwright 视觉回归通过(零 console error,KPI/CTA/进度条/工具按钮 V14.92 改动全部保留)。

> V15.1 月报与事件重合补弹:
>
> 修复第 31/61/91 天月报和 7 天随机事件重合时,随机事件优先弹出后导致月报永久错过的问题。月报触发改成按 `monthCounter` 判断到期日的可重试逻辑:日切时若没有事件会正常弹月报;如果当天先弹随机事件或投资人事件,玩家处理完事件后会立刻补弹当月经营报告;如果存档已经越过到期日,下一次 tick 也会补弹,并把该事件的现金影响纳入当月月报。

> V15.0 8× 自适应空白压缩:
>
> 8× 倍速下,如果上一 tick 没有任何"有事"信号(完单、日切、口碑变化、剧情触发),下一 tick 间隔从 250ms 压到 50ms;有事则保持 250ms。其他档位(1×/2×/4×)维持固定间隔不变。底层从 setInterval 改为 setTimeout 链式调度,通过 useRef 记录上一 tick 关键状态快照来判定"有事"。实测开局 8× 下 1 游戏天耗时从 6 秒降至约 4-4.5 秒,夜间空白时段被显著压缩,白天忙碌时段保持原节奏。改动只动了 React 调度层(网约车物语-V3.html 的 useEffect 和 src/app/90-app.jsx 同步副本),wycwy-engine.js 不变,不影响数值平衡。

> V14.89 结构化复盘日志:
>
> 每局新增 `actionHistory` 结构化复盘日志,和右下角运营日志分离。右下角日志仍用于玩家即时阅读,`actionHistory` 用于后续破产复盘和数值分析:会记录玩家开始/暂停/倍速、关闭引导、抽卡关闭、月报关闭、故事确认、主动结束运营等操作,也会同步记录所有关键运营日志和通知对应的资金、口碑、车队、任务进度、月薪、高利贷等快照和变化差异。游戏结束自动保存的 `wycwy-run-history-v1` 也会带上最近 300 条结构化复盘日志。

> V14.88 事件触发与月报遮挡修复:
>
> 随机事件触发时立即写入“事件出现:xxx”运营日志,方便月报复盘时确认事件确实在第几天出现过。事件触发后 tick 立即返回,不再继续生成月报,避免未来遇到事件日和月结日重合时月报盖住事件弹窗。月报的事件明细增加风险兑现/未兑现说明,例如暴雨抢单事故会展示事故日志,不再只显示“暴雨事件 + 金额”。

> V14.87 每局自动保存复盘日志:
>
> 游戏结束或破产时自动保存一份运行记录到 `localStorage` 的 `wycwy-run-history-v1`,最多保留最近 20 局。记录包含结果/破产原因、天数、资金、口碑、任务进度、司机与车辆、月度经营数据、最近 120 条运营日志和最近 240 条诊断轨迹。手动“诊断”导出也复用同一份诊断结构,之后复盘破产原因不再依赖玩家当场截图。

> V14.86 主线任务与订单经济重调:
>
> 主线任务从 7 步扩为 16 步,按“完成第一单 → 跑完整个运营日 → 买车 → 招司机 → 组车组 → 服务训练 → 口碑 90 → 车技 35 → 凯美瑞 → 口碑 180 → 专车订单 → 5 车组 → 奔驰 E → 车技 70 → 口碑 520 → 豪华车订单”推进。第一批订单任务加入“运营到第 2 天”的时间门槛,避免第一单后几秒内连续跳任务。订单价格重调为特惠 ¥48、快车 ¥88、专车 ¥220、豪华车 ¥360,并新增 `GAME.MIN_ORDER_FARES`,游戏加载调参缓存时会把过低订单价抬回健康底线,避免手动调价导致第一个月司机普遍负贡献。

> V14.85 查看目标改为任务牵引:
>
> “查看目标”弹窗不再把“订单目标”作为独立 tab。第一 tab 改为“任务路线”:顶部展示当前主线任务、任务提示;下方展示主线任务路线。专车订单、豪华车订单被收进对应任务节点,只在任务需要它们时展示片区、司机属性、车型三类条件。这样订单解锁成为任务推进的一部分,玩家不需要先理解一张完整订单大全。第二 tab 保留“游戏目标”,继续承载结局路线。

> V14.84 调参缓存保留事件函数:
>
> 修复 `wycwy-config-override` 实时调参缓存覆盖事件数据时,会把 `EVENTS[].options[].apply` 这类嵌套函数丢掉的问题。游戏页和 admin 调参页的覆盖合并逻辑都改为递归合并:文案、冷却、数值等 JSON 字段可以继续覆盖,但任何函数都会从原始数据里保留。这样即使本地已有旧调参缓存,事件弹窗也能恢复正常预览和结算,不会再把所有选项显示成“事件预览异常”。

> V14.83 事件预览与结算兜底:
>
> 修复事件弹窗可能出现“文案有资金/忠诚变化,预览却显示无立即数值变化”的问题。预览函数如果遇到异常,不再返回空效果并显示“无变化”,而是显示“事件预览异常”。事件结算也加 try/catch 兜底:即使某个事件选项 apply 异常,也不会把页面打白,会关闭事件并写入警告日志/通知。`无立即数值变化` 只在没有 effect 且 detail 也没有任何数值语义时出现。

> V14.82 投资人压力硬扛选项说明:
>
> 投资人压力弹窗的兜底按钮从“再等等看 (N 天后破产)”改为“不采取措施,靠流水回正”。按钮下方增加风险说明:该选项适合玩家判断订单流水足够强、现金很快能转正的情况;如果 N 天内资金没有回正,投资人撤资并游戏结束。对应日志和通知也改为“赌接下来靠订单流水回正”,避免玩家误以为这是一个没有后果的等待选项。

> V14.81 供需轴改为连续风险渐变:
>
> 顶部供需状态条从“左黄 / 中绿 / 右红”的三段色块改为连续渐变风险轴:左侧绿色代表供给充足和安全,中间黄色代表接近吃紧,右侧红色代表供给不足。指针算法同步从天平式 `过剩 ↔ 平衡 ↔ 不足` 改成风险式 `充足 ↔ 临界 ↔ 不足`,正常匹配会落在偏左绿色区域,订单流失越多越向右移动。

> V14.80 信任突破来源收窄:
>
> 信任突破只保留在少数强关系事件:重大私人危机、严重事故公司担责、司机被抢/被冤时公司站队、个人故事奖励。普通借钱、红包、聚餐、社保、制度性补贴、小剐蹭赔偿都回到普通忠诚,只受普通忠诚上限限制。突破上限后的忠诚如果再下降,没有特殊保护,按当前忠诚正常扣减。

> V14.79 投资人压力动态救场:
>
> 投资人压力不再固定为“裁 1 人 / 卖 1 车 / 借 ¥10,000”。系统会根据当前司机数、车辆数、可运营车组和负债缺口生成救场方案:早期仍以裁 1 人、卖 1 车为主;中后期按约 30%-35% 车队规模裁撤高薪司机、出售高价车辆,例如 10 人车队会给出裁 3-4 人、卖 3-4 车的一揽子止血方案。高利贷本金按当前负债缺口生成,约为缺口的 2-3 倍,并随金额提高利息和缩短还款期;已有高利贷会合并到待还金额里。

> V14.78 训练费用按属性段位递增:
>
> 车技和服务训练不再使用单一固定价格,改为按司机当前属性分段计价。车技价格为 0-19:¥300、20-34:¥450、35-49:¥700、50-69:¥1,100、70-84:¥1,700、85+:¥2,600;服务价格为 0-19:¥350、20-34:¥500、35-49:¥800、50-69:¥1,250、70-84:¥1,900、85+:¥2,900。训练增益仍保持 +8~12,让前期冲专车门槛不拖沓,后期冲豪华车和顶级属性形成资金压力。右侧能力训练行展示并扣除当前这一次训练的实际价格。

> V14.77 月报收支结构重组:
>
> 月度经营报告不再把正向事件现金流放进“支出构成”。月报拆成“收入构成 / 支出构成 / 其他现金变动”:支出只放月结发薪、债务扣款、解雇补偿等固定现金流;卖车回血、借款、事故赔付、事件奖励或罚款统一进入“其他现金变动”,并记录第几日、事件名、玩家选择和金额。这样月底看到 `+¥9,000` 时能追溯来源,不会把正数误读成支出。

> V14.76 司机属性只通过训练成长:
>
> 删除完单后随机提升车技 / 服务的 `levelUpStats` 逻辑,司机跑单只增加订单数、收入、好评和故事进度,不再自动涨属性。司机故事里程碑也不再发放属性奖励,避免玩家不花钱也能慢慢把普通司机养满。车技和服务的成长入口统一收敛到右侧“能力训练”里的付费 `+` 按钮,让“花钱训练 → 解锁高价订单 / 提升好评率”的因果更清晰。

> V14.75 忠诚分层与离队士气:
>
> 司机忠诚开始跟稀有度绑定:级别越高初始忠诚越低、普通忠诚上限越低、进入离队风险的阈值越高。普通福利 / 补贴 / 制度投入只受“普通忠诚上限”限制;少数重大私人危机、严重事故公司担责、替司机申冤、个人故事等强关系选择走“信任突破”,可把忠诚推到 100。因低忠诚主动离队时,剩余司机会受到团队士气影响,按离职司机稀有度扣少量忠诚,单日最多扣 10。

> V14.74 新手引导第二步高亮收窄:
>
> 新手引导第 2 步“先看钱和口碑”的 spotlight 不再圈住整条顶部栏,改为根据 `.topbar-kpis` 实际位置动态测量,只高亮中间的时间、资金、口碑、供需状态板。左侧游戏名和右侧工具按钮不再被圈入,避免用户误以为这些也是当前步骤要关注的操作。

> V14.73 右侧车组详情分组重组:
>
> 右侧车组详情改成“车组概览 → 常用操作 → 能力训练 → 风险操作”的固定顺序。换车 / 分配车辆入口前移到常用操作区,并改成更明确的主按钮;训练属性独立成能力训练区;解雇和卖车统一收进风险操作区。玩家选中司机后先看到当前人车关系和下一步可做动作,低频破坏性操作不再和日常操作混在一起。

> V14.72 事件弹窗状态与选项对比增强:
>
> 事件弹窗里的经营状态条进一步降权为轻量账本行,只用虚线和浅底承载当前资金、口碑、流水和车队规模。两个事件选项改成默认有厚边框、硬阴影和“选择”角标的按钮卡,hover / focus 时提高边框与阴影强度,让“当前数值”和“可点击决策”在未选中状态下也能明显区分。

> V14.71 事件决策弹窗头部重组:
>
> 随机事件和投资人压力弹窗把“事件类型 / 主标题 / 事件描述”合并成连续头部,经营状态从 4 张大卡片改成轻量状态条,放在事件描述下方、选项上方。玩家先读懂发生了什么,再带着资金、口碑、今日流水和司机/车辆规模做选择;状态信息不再割裂标题与说明。

> V14.70 换车操作弹层化:
>
> 车组详情里的“换车 / 互换”不再常驻展示车辆网格。右侧面板只保留紧凑的“车辆管理”入口,点击后在居中弹层里选择空车、当前车或其他司机车辆,完成分配/互换后自动关闭弹层。低频操作被收纳,右侧详情优先留给司机属性和常用管理信息。

> V14.69 事件弹窗去内部机制:
>
> 随机事件弹窗和事件日志不再展示“基础效果”“按当前规模 ×N 结算”“规模系数 ×N”等内部结算信息。动态事件仍按当前经营规模结算,但玩家只看到最终资金、口碑、忠诚、抽成、临时收入变化和事故风险等可理解结果;订单收入变化从倍率写法改成“临时提高 / 降低 N%,持续 N 天”。

> V14.68 任务完成反馈收据化:
>
> 普通任务完成反馈去掉左侧“任务”图标和重黑底标签,改成紧凑收据结构:状态、任务名、说明、奖励行和“领取奖励”按钮。减少“任务 / 任务完成 / 完成第一单”重复堆叠,让玩家先看到完成了什么和拿到什么。通关达成仍保留居中强提醒。

> V14.67 第二轮死代码 + 死 CSS 大扫除 + P0 修复:
>
> **真 bug 修复**:`--ink-1` CSS 变量全局缺定义(`:root` 加兜底);月报抽成"(20%)"硬编码改成动态;`floatGains` / `notifications` 清理 effect 改用 ref 跟踪 id 防漏触发;`SFX.warn` 改用 ref 防误响;投资人事件通知里的破产倒计时加 `bankruptcyGraceBonus`;删除 EventModal 里 V14 借贷重构后已不存在的 `debtInstallment` 死分支。
>
> **死代码大扫除**:
> - 引擎层:删除 `TRAIN_TO_TARGET` / `HIRE_DRIVER` / `RESET_STORY_MEMORY` 三个 reducer case + `doTrainToTarget` / `hireDriver` / `getNextTrainingTarget` / `TRAINING_TARGETS` / `updateZoneHeat`;driver 死字段 `exp` / `rankId` / `bgDesc` / `constraint` / `todayHistory`;state 死字段 `zoneHeat` / `kpiHistory` / `yesterdayLost|Earned|Completed`;vehicle `policyCertifiedDay`;data 层 `ORDERS.icon` / `LAST_NAMES_M` / `SPARKLINE_LEN`;`resolveEvent` 里 `promoteBest` / `fireMostExpensive` / `sellMostExpensive` 三个死分支(events 数据已无产出)。
> - HTML 层:TopBar 5 个未用变量;训练提示硬编码"专车需车技35"改为从 `ORDERS.req` 动态推导;"¥10,000" 改 `GAME.STARTING_FUNDS`。
> - **死 CSS**:用受控解析脚本一次性删 303 个规则,~2050 行 / ~41 KB。涉及老调度卡 `.dc-*` / `.dco-*` / `.vc-*`、老抽屉 `.drawer-*`、老训练 `.training-card-*` / `.stat-train-*`、老忠诚卡、老 mb/hud、老 fleet、老抽卡、老 mini 弹窗、老车辆装配、老教程、老故事、老像素小人等。
>
> **admin.html schema 同步**:exports 删 `LAST_NAMES_M` / `computePlayerPhase`;RECRUIT_TICKETS 描述去掉"按玩家阶段";MISSIONS 描述 "12 条" → "7 条";EVENTS 描述更新为 V14 + 全部 2 选项;新增 ZONES 编辑表(unlock.reputation / density / orderMix.x4)。

> V14.66 属性说明文案压短:
>
> 司机属性说明统一改成一行短句:忠诚说明“越高越愿接单,过低可能离职”;车技说明“专车需 35,豪华车需 70”;服务说明“越高,好评率和口碑涨得越快”。不再在属性说明里动态展示差多少,避免玩家读起来费劲。

> V14.65 司机属性与操作区重组:
>
> 右侧车组详情把“忠诚 / 车技 / 服务”统一成同一种属性行:每行展示属性类型、当前值 / 上限、进度条和影响说明。忠诚上限固定 100 且不可手动升级;车技和服务保留 `+` 训练按钮。删除“个人统计”区,右侧下半部分聚焦换车/互换、解雇和卖车等操作。

> V14.64 右栏 hover 阴影防裁切:
>
> 右侧车组详情滚动区为“查看 / 分配 / 换车 / 互换”等交互按钮统一预留左右和底部缓冲,换车卡片组自身也预留局部内边距。此前这些按钮 hover 会向左上位移、并向右下长出 2-3px 像素硬阴影,但父级滚动区横向裁切,贴边时阴影会显示不全。

> V14.63 订单目标状态层级:
>
> “查看目标 → 订单目标”里的全部订单列表调整状态权重:已解锁订单改为弱背景、弱文字和无边框状态文本,作为完成态退到背景;未解锁订单保留硬边框、强调价格和黑底金字状态,作为玩家当前需要关注的目标。

> V14.62 接单日志合并:
>
> 司机开始接单时不再单独写入运营日志。完单时生成一条结果日志,合并展示司机、片区、订单、收入以及好评/投诉/口碑变化,避免“接单一条 + 完单一条”导致日志高速滚动看不清。

> V14.61 复古滤镜亮度修正:
>
> 调低滤镜暗角和像素点透明度,并取消扫描层的强 `multiply` 叠加。滤镜仍保留横向扫描线、竖向栅格和轻微纹理,但不再明显压暗整张画面,降低长期游玩时的灰暗感。

> V14.60 供需说明白话化:
>
> 供需 HUD 不再暴露“采样中”。运营初期数据不足时先按“匹配正常”展示,等最近几小时数据足够后再判断“供给不足 / 供给充足”。供需说明改成玩家语言:供需就是司机够不够接订单;司机太少、车不够或车型不对会导致订单流失,流失会扣城市口碑;司机多订单少则说明供给充足但有人闲着不赚钱。

> V14.59 列表 hover 阴影防裁切:
>
> 左侧车队列表的滚动容器为卡片 hover/selected 硬阴影预留左右和底部缓冲。此前 `.compact-card:hover` 会加 2px 硬阴影,但 `.compact-list` 只预留了 2px 右侧滚动条空间,导致卡片靠左/靠右时边缘阴影被裁掉;现在通过列表内边距 + 负外边距保持视觉位置不变,同时让阴影完整显示。

> V14.58 顶部 HUD 规则说明:
>
> 顶部时间、资金、口碑、供需四个核心指标统一支持悬停/键盘聚焦说明。时间说明展示 1× 与游戏小时的关系、倍速影响、日结和 30 天月结;资金说明展示收入来源、主要支出、工资月结和破产风险;供需说明展示采样逻辑、匹配正常、供给不足和供给过剩的判定。

> V14.57 复古滤镜加强:
>
> “滤镜”不再只有很浅的横向扫描线。开启后叠加横向扫描线、竖向栅格、轻微像素点纹理和暗角,更接近模拟经营游戏常见的像素屏/老显示器质感。滤镜仍作为可关闭效果,避免长时间游玩影响文字识别。

> V14.56 顶部 HUD 去彩条:
>
> 顶部时间、资金、口碑、供需四个指标不再用底部黑/橙/绿/灰彩色线条表达状态。保留外框、竖向分割和数字颜色即可完成分组与重点表达,减少顶部 HUD 的横线噪音。

> V14.55 SSR 赛车名扩展:
>
> SSR 姓名从单独赛车名人改成“赛车名人 + 称号后缀”的组合,当前为 12 个赛车名人 × 4 个称号后缀,共 48 个 SSR 可识别姓名。这样仍然保留一眼传说级的赛车名人感,同时避免后期猎头券反复抽到同名司机。

> V14.54 车组订单一行全展示:
>
> 车组卡与右侧车组详情顶部隐藏车型文字,由车辆 icon 承载车型识别;车辆旁边直接一行展示全部可接订单短名,如 `特惠·快车·专车·豪华`。完整车型名称仍在换车/互换和车辆详情里出现,避免主卡片被“车型名 + 4 类订单”挤成多行。

> V14.53 目标弹层高度稳定:
>
> “查看目标”弹层的订单目标 / 游戏目标 tab 共用固定高度内容区。订单目标内容较多时只在内容区内部滚动,游戏目标内容较少时保留稳定留白,切换 tab 时弹窗外框高度不再跳动。

> V14.52 司机姓名等级化:
>
> 司机姓名开始直接承载稀有度识别:N 级生成两个字普通姓名,R 级生成三个字完整姓名,SR 级生成“金牌/王牌/首席”等前缀姓名,SSR 级直接使用赛车名人式姓名,如舒马赫、塞纳、汉密尔顿、维斯塔潘。抽卡候选会尽量避开当前车队已有姓名和同屏重复姓名,让玩家不看标签也能从名字判断等级。

> V14.51 查看目标降噪:
>
> 目标弹层副标题改为游戏式指令文案“选择目标类型,查看下一步推进条件”。游戏目标 tab 删除重复的“当前游戏目标”说明卡;主线结局列表不再提前展示结局故事描述,只保留已揭晓结局名、达成条件和状态,后续结局继续用问号隐藏。结局故事只在结算/结束弹窗中展示。

> V14.50 车组常态状态降噪:
>
> 车组卡和右侧车组详情不再展示“等待接单 / 正在接单”这类常态状态。自动接单游戏里,这两个状态不能直接指导玩家操作,且已经被地图小车和日志表达;界面只保留“休息中 / 未配车 / 空车”等需要玩家处理或理解的异常状态。

> V14.49 后期事件痛感增强:
>
> V14.46 的事件动态系数过于保守,在 ¥200,000 资金 / 口碑 500 / 8 车组的后期状态下仍不足以影响决策。经营规模系数上限从 5× 提到 15×,资金和口碑对系数的贡献加倍,负面口碑惩罚按 120/300/500/800 档进一步放大。后期小事故、借钱、人事和公关类事件会进入数万资金或数十口碑的量级,但开局仍基本保持原值。

> V14.48 查看目标双 Tab:
>
> 左侧入口从“查看路线”改为“查看目标”。目标弹层拆成“订单目标 / 游戏目标”两个 tab:订单目标只解释下一档高级订单如何解锁;游戏目标只展示已达成结局和当前可冲击结局,更后面的结局用问号隐藏,避免一次性剧透所有终局条件。

> V14.47 结局路线前置:
>
> 五种结局的结论重新梳理为“个体户 → 区域品牌 → 全市 Top 3 → 被巨头收购 → IPO 上市”的经营成长线。阶段 3/4 不再用 SR/SSR 司机作为隐藏门槛,改为要求豪华车组数量,并在目标弹层中新增“经营结局”区块,让玩家提前看到结局的故事结论和达成条件。

> V14.46 事件动态惩罚:
>
> 随机事件的资金、口碑和加薪影响不再只按固定值结算。引擎会按当前可运营车组、现金规模和城市口碑计算经营规模系数,再把资金影响取整到 ¥500/¥1,000/¥5,000 档,口碑扣分按口碑档位放大。事件弹窗预览和真实结算共用同一套 `scaleEventEffect`,后期资金和口碑更高时事件仍有经营压力。V14.49 已把后期上限和资金/口碑权重进一步拉高。

> V14.45 区域品牌结局条件去稀有度化:
>
> 阶段 2“区域品牌”不再要求“R+ 已配车司机”,改为要求至少 1 个专车车组(凯美瑞/奔驰 E + 驾驶 ≥35 的已配车司机)。结局条件从抽卡稀有度转向玩家更容易理解的订单能力,文案同步删除“R 级司机镇场”。

> V14.44 口碑规则前置:
>
> “口碑怎么涨/怎么降/如何影响片区解锁”从订单解锁路线弹窗底部移到顶部口碑 HUD。玩家悬停或聚焦口碑区域即可看到好评累计、投诉扣分、订单流失扣分、片区门槛和下一片区进度,避免在目标路线弹窗里塞入额外说明块。

> V14.43 薪酬与解雇成本重调:
>
> 司机月薪整体上调到更像经营成本的区间:N 级约 ¥3,200-4,200,R 级约 ¥6,500-7,200,SR 级约 ¥11,000-12,000,SSR 级约 ¥18,000-20,000。主动解雇司机需要支付 2 个月薪资补偿,现金不足时不能解雇;补偿会进入月报支出。竞品挖人和转行挽留事件的加薪幅度同步上调,避免新薪酬体系下加薪没有存在感。

> V14.42 车组卡信息降噪:
>
> 左侧车组卡不再用边框标签展示“等待接单”和“已赚金额”,改为弱化的状态/收入摘要,避免看起来像可点击按钮。车辆可接订单从“2 类订单”改为直接展示“特惠、快车”等具体订单类型,减少玩家理解成本。右侧车组详情删除与顶部摘要重复的“车辆与订单”大卡片,司机视角只保留换车/互换操作;单独选中空车时仍展示车辆与订单详情。

> V14.41 工资改为月结:
>
> 司机工资不再每天直接从现金扣除。日结只累计“本月应付工资”,进入第 31/61/91 天生成月报时一次性发薪并扣现金。月报中的司机贡献改为按本月实际应付工资计算,中途招募、离队、加薪都会影响本月工资,而不是粗暴扣整月月薪。

> V14.40 训练区信息去标签化:
>
> 能力训练区不再用额外标签展示“专车/豪华车差多少”和“好评率”。驾驶、服务等关联信息统一放回能力行内部,以一句说明文字解释“训练这项能力会影响什么”,减少红框标签和重复边框造成的视觉噪音。

> V14.39 忠诚度显性化:
>
> 忠诚不再只是事件里的隐藏数值。左侧车队卡显示司机忠诚,右侧车组详情展示忠诚条、状态和实际效果。忠诚 50 为接单意愿基准,低忠诚会降低接单意愿;V14.75 起离队风险线按稀有度区分。这样事件里的“全员忠诚 ±N”和加薪挽留能形成真实经营后果。

> V14.38 标签体系极简化:
>
> 将审阅页确认的极简按钮/标签规则同步回游戏主界面:右上角工具区从符号按钮改为“帮助 / 诊断 / 声音 / 滤镜”文字按钮;订单类型、稀有度、路线状态、能力条件不再各自彩色化,统一为“主标签黑底金字、普通信息白底黑字或弱文本、异常风险红色”三类视觉语义。

> V14.37 片区订单列表固定排序:
>
> 右侧片区信息里的订单列表不再按出现频率动态排序,统一按特惠订单 → 快车订单 → 专车订单 → 豪华车订单展示。频率仍显示为百分比和高频/常见/少量,但位置稳定,方便玩家在不同片区之间横向比较订单结构。

> V14.36 车型未解锁订单不计供需:
>
> 片区仍可按 `orderMix` 生成专车/豪华车等潜在高阶需求,但在玩家没有任何可接车型前,这些订单不进入自动派单池、供需统计、订单流失和口碑扣分。只有买到凯美瑞后专车订单才开始计入供需压力,买到奔驰 E 后豪华车订单才开始计入供需压力,避免开局玩家因无法购买的车型被隐性扣口碑。

> V14.35 基础设计语言统一:
>
> 按“像素经营游戏”的方向统一按钮、标签、弹窗、卡片和调度台描边:通用按钮改为 2px 硬边框 + 硬阴影 + 下压反馈;标签统一为低圆角/无圆角硬标签;故事和目标路线提示从左竖线样式改为完整硬边框卡片;补齐 `--line-ui` 设计 token,避免描边风格依赖浏览器兜底。

> V14.34 调度台默认选中车组:
>
> 右侧调度台不再把“从左侧选择车组”作为开局默认体验。详情页没有选中片区或车组时,系统会自动选中第一个可运营车组;当前选中对象不存在时也会回落到可用车组。空状态只作为没有司机和车辆时的兜底。

> V14.33 去掉口碑保护期:
>
> 删除开局订单流失保护期。订单流失从第一天开始就按 1 单 = 城市口碑 -1 生效,顶部只展示真实扣分,不再出现“保护期潜在扣分”这类难解释状态。

> V14.32 城市解锁节奏重调:
>
> 开局只解锁海甸区,其余片区按熙城 90、风台 180、大馨 340、昭阳 520 的口碑台阶逐步开放。口碑增长从“每个好评 +1”改为“每 3 个好评沉淀 +1 口碑”,避免自动跑单后风台、大馨、昭阳几乎同时解锁。

> V14.31 早期任务节奏放缓:
>
> 第二个任务从“累计 8 单立即完成”改为“累计 12 单且运营到中午后完成”,避免开局连续弹出两次任务完成。任务完成反馈从 3.5 秒延长到 8 秒,且上一条完成反馈未关闭前不结算下一条任务,让玩家有时间读懂奖励和下一阶段。

> V14.30 新手引导重写:
>
> 首次引导从“介绍界面组成”改成“带玩家完成第一轮操作”:开局资源 → 顶部资金/口碑 → 左侧任务 → 左侧车组 → 地图开始运营。文案减少术语解释,只告诉玩家现在看什么、点哪里、为什么重要。引导 UI 改为硬边框教练卡、金色 spotlight、左侧强调条和步骤进度条,与当前像素经营游戏风格保持一致。

> V14.29 片区订单结构改造:
>
> 片区不再绑定单一或少数订单类型,改为每个片区都有 4 类订单的 `orderMix` 权重。初级片区特惠订单占比高,高级片区快车/专车/豪华订单占比高,让“解锁新片区 = 更高收入结构”更容易理解。右侧片区信息去掉顶部单个金额和“接单规则”说明,简介改为无外框文本,片区订单列表展示每类订单的价格区间和出现占比。

> V14.28 任务反馈与地图车辆稳定性:
>
> 任务完成时取消全屏白色 `flash-overlay`,只保留右上角任务完成反馈卡,避免弹窗出现时整屏闪一下。地图车辆不再按订单小时进度在区片周围大幅跳点,改为每名司机在片区内使用稳定车位 + 极轻微浮动,8× 倍速下不会因 0.3 秒一 tick 出现车和收益飘字对不上的跳跃感。

> V14.27 收益反馈单点化:
>
> 收入动效统一收敛到地图:删除接单中常驻绿色预计金额,只在完单入账时显示橙色 `+¥金额` 飘字;左侧车队卡不再触发 `+¥` 飘字、金币粒子和卡片震动,只保留累计已赚数字变化。地图车辆旁的司机首字徽章缩小,仅作为归属提示,避免压住车辆和收益飘字。

> V14.26 顶部经营状态 HUD 重做:
>
> 顶栏中间 4 个经营状态从分散胶囊改为一个连续状态板:外层统一硬边框和像素硬阴影,内部四格按时间、资金、口碑、供需分区。每格只突出“标签 + 主值”,阶段、供需提示、口碑流失等副信息降级为轻量角标或底部小提示;供需三段轴隐藏文字刻度,只保留小型状态条和指针。目标是让玩家优先扫到当前天数、现金、口碑和供需状态,减少顶部信息碎片感。

> V14.25 地图标签降噪与运行按钮 hover 修复:
>
> 地图小车已经能表达司机正在跑单,因此移除叠在区片上的“运营中 N”标签,避免地图同时出现车辆、收益、运营标签三层信息。修复底部“继续运营/暂停”主按钮 hover 时沿用普通按钮 hover 色导致文字对比不足的问题,主运行按钮 hover 仍保持深底金字,首次开始按钮 hover 保持橙底白字。

> V14.24 地图收益飘字轻量化:
>
> 地图上的收入反馈从“白底描边框 + 数字”改为游戏式轻量飘字。接单中预计收入只保留小号绿色数字和浅描边;完单收入只显示金色 `+¥金额`,去掉外框和司机首字,司机归属由车旁首字徽章承担,避免地图上出现过多小弹窗。

> V14.23 地图跑单车辆资产化:
>
> 地图上的跑单对象从 SVG 小矩形车改为当前车组对应车型 PNG。地图主视觉用车表达“哪档车在跑单”,车身旁保留一个小司机首字徽章表达归属,比直接放头像更符合地图语义,也能让高端车/普通车的差异在运营画面里持续可见。

> V14.22 运行控制三态化:
>
> 地图底部运行控制改为三态:开局未开始时只显示一个“开始运营”主按钮,按钮内标注“1× 正常速度”,不提前展示 2×/4×/8×;游戏开始后,运行态显示“暂停 + 1×/2×/4×/8×”;暂停后显示“继续运营 + 1×/2×/4×/8×”,此时点击任意倍速会直接按该速度继续运营。视觉上改为硬边框、硬阴影、金色选中态,和当前像素经营游戏 UI 保持一致。

> V14.21 三档车型视觉重做:
>
> 重新生成并裁切 3 张当前实际车型 PNG:经济档是米色老旧方正轿车,中端档是绿色现代商务轿车,高端档是黑金长轴豪华轿车。车辆组件显示比例从旧占位图的窄比例调整为横向车身比例,左侧车队卡车辆列加宽,买车弹窗图片区加高,避免车身被裁切。

> V14.20 生图像素资产替换:
>
> 使用生图工具生成统一像素美术素材板,从本地生成图裁切出 10 张司机头像 PNG 和 5 张车辆 PNG,并对白底做透明化处理。运行时 `DriverAvatar` / `VehicleIcon` 改为加载 PNG 位图资产,不再加载上一版程序化 SVG。旧 SVG 文件保留为可回退素材,但界面默认使用生图 PNG。

> V14.19 车辆像素图去容器框:
>
> 车辆已经从 CSS 占位块升级为独立像素图片,因此移除 `.asset-vehicle` 外层黑框和阴影,只保留车辆图片本身。头像仍保留圆形边框,因为头像需要在列表中保持人物识别和点击热区。

> V14.18 像素角色与车辆资产:
>
> 新增 `assets/pixel/avatars/` 和 `assets/pixel/vehicles/` 本地 SVG 像素资产,覆盖 10 种司机背景和 5 种车型。司机头像与背景 id 绑定,车辆图标与车型 id 绑定,主界面、招募卡、车队卡、调度台会统一展示同一套像素图片;旧存档里没有头像 asset 字段时,按帽子类型自动回退到对应头像,避免刷新后丢图。

> V14.17 地图收益反馈:
>
> 司机接单中,地图小车头顶持续显示本单预计净收入;完单时在对应片区弹出“司机首字 +¥收入”的短飘字,左侧车队卡片同步显示累计已赚金额和完单瞬时飘字。区片中心圆圈不再只放裸数字,改为“需求 + 数值”;订单层只在司机运营时显示“运营中”,避免同一区片出现两组需求数字。

> V14.16 任务卡主副标题化:
>
> 左侧任务卡删除“下一步 / 提示”字段标签,改为“任务 N/7 + 查看路线按钮 + 主标题 + 副标题”的游戏目标提示结构。主标题使用任务名,副标题使用当前达成条件或操作说明;原“目标路线”按钮改为“查看路线”,点击含义更直接。长期路线、订单门槛和解锁原因仍放在目标路线弹层里,任务卡只负责告诉玩家眼前要完成什么。

> V14.14 日志时间线规则:
>
> 右侧日志 tab 改为游戏常见的时间正序:旧日志在上、新日志追加到底部。进入日志页或玩家停留在底部时,新日志会自动跟随到底;如果玩家手动上翻查看历史,不强制打断阅读。最新一条保留轻量高亮,底部提供“回到最新”按钮。

> V14.13 任务卡信息降噪:
>
> 左侧任务卡只保留“任务 N/7、目标路线入口、下一步动作、单行提示”。删除单独任务标题和底部 7 段进度条,避免“任务 3/7”和进度点重复,也避免任务标题与动作文案互相重复。下一步和提示统一为两列表格式,左侧标签定宽、右侧内容对齐,减少白框/标签/按钮多种样式混用。即时操作提示留在任务卡外显,长期路线、订单门槛和解锁原因继续放进“目标路线”弹层。

> V14.12 订单解锁逻辑重排:
>
> 订单解锁改成更容易理解的经营链路:特惠是默认现金流;快车只由片区解锁,桑塔纳也能跑;专车需要凯美瑞/奔驰 E + 驾驶 ≥35;豪华车需要奔驰 E + 昭阳等高端片区 + 驾驶 ≥70。服务不再直接卡订单类型,只影响好评率和口碑增长,口碑再推动片区解锁。
>
> 为避免驾驶变成单纯门槛值,专车和豪华车单价改为随驾驶值平滑增加:专车从驾驶 35 起逐步增加,最高 +25%;豪华车从驾驶 70 起逐步增加,最高 +20%。训练从“补到一个线”变成“越强越赚钱”。

> V14.11 调度台日志与地图悬浮控制:
>
> 事件日志从底部 HUD / 抽屉迁移到右侧调度台,调度台顶部新增“调整 / 日志”两个 tab:调整页承载片区、车组、训练和换车信息,日志页承载完整运营记录。底部通栏控制条不再渲染,开始/暂停与 2×/4×/8× 倍速按钮改为城市订单地图底部的悬浮控制组,让运行控制只占地图区域,不再横跨整屏。

> V14.10 车组详情整合:
>
> 右侧车组详情顶部改成和左侧车队卡一致的人车分组:司机头像 + 姓名/背景放在一起,车辆图标 + 车型/可接订单放在一起,状态统一固定在右侧。能力训练下方不再展示长解释句,改用订单状态标签:属性未达标显示“差 X”,属性达标且当前车能接显示“可跑”,属性达标但车型不支持显示“需换车”。这样训练、车辆可接订单、换车和统计围绕同一个车组关系展开。

> V14.9 顶栏经营状态整合:
>
> 顶栏中轴统一展示 4 个全局状态:时间、资金、口碑、供需。原本底部 HUD 的“供给过多/供给不足”状态轴上移为“供需”胶囊,与口碑相邻展示,强化“订单流失会影响口碑”的关系;左侧时间也移入同一组状态胶囊,不再和品牌标题绑在一起。底部 HUD 回归操作区,只保留事件日志和开始/倍速控制。

> V14.8 买车/招募自动补齐车组:
>
> 买车后如果已有未配车司机,新车自动分配给最早的未配车司机;招募司机后如果已有空车,新司机自动开上最早的空车。自动配对只补空缺,不抢已配好的车组,并写入系统日志,让“买车再招人 / 招人再买车”的常规路径少一步手动配车。

> V14.7 任务卡移除孤立阶段目标:
>
> 左侧任务卡删除“阶段目标:个体户 ¥2.5万 · 口碑160 · 车组3”等长期结局条件,只保留当前任务标题、当前动作、辅助提示、7 段主线进度条和“目标路线”入口。长期目标改由目标路线弹层承载,避免任务卡同时承担眼前动作和长期 KPI 两种语义。

> V14.6 车队列表信息层级收敛:
>
> 左侧车队标题区删除“点击司机训练/配车”和待处理汇总,只保留车组数与招募/买车入口;空车/未配车信息交给列表卡片自身承载。车组卡从“头像和车图并排 + 名字和车型并排”改成“司机头像 + 司机名”“车辆图标 + 车型信息”两行分组,状态统一放到右上角。

> V14.5 车队头部信息排布修正:
>
> 左栏“车队”头部从“标题/说明/按钮挤在同一块”改为两层结构:第一行只放车队标题和“招募/买车”操作,第二行单独展示车组状态与操作提示。说明文案改为 `点击司机训练/配车`,去掉斜杠两侧空格,避免在窄左栏里被挤成多行。

> V14.4 招募券弹窗高度微调:
>
> 招募新司机的三张招募券仍保持桌面横向对比,但券卡加高并增加内部上下间距。标题/价格、券说明和“抽卡 3 张”按钮保持同列对齐,避免横排卡片显得过扁;窄屏下选券页改为单列,避免三列卡片被裁切。

> V14.3 左侧任务卡布局优化:
>
> 任务看板继续放在左栏顶部,但从“信息堆叠卡”改成“当前动作卡”。顶部只放任务序号和状态标签,中间突出当前要做的动作,辅助提示与阶段目标降级展示;底部增加 7 段主线进度条,并把“下一目标”按钮改成较轻的“目标路线”入口。目标是让玩家先看到下一步动作,再看长期目标,不和下方车组列表抢主操作层级。

> V14.2 购车弹窗对比布局:
>
> 4S 店买新车弹窗从两列瀑布式改为三车型横向对比。桑塔纳、凯美瑞、奔驰 E 在同一行展示,车型图、名称/价格、可接订单数量、订单标签和购买按钮按同一节奏对齐,方便玩家比较价格阶梯和订单覆盖差异。

> V14.1 城市地图素材底图接入:
>
> 城市地图从纯 SVG 色块底图升级为“清新地图素材底图 + SVG 交互覆盖层”。底图保存为 `assets/maps/city-map-clean-v1.png`,只承载地形、河流、道路和少量地标;解锁状态、点击热区、锁定遮罩、区片标签、小车跑单和热点标记仍由代码动态绘制。
>
> 区片位置按底图重新对齐:海甸在左上居住/高校区,昭阳在右上商务区,熙城在中心城区,大馨在左下机场区,风台在右下交通枢纽区。风台/大馨的机制含义不变,只是视觉位置与底图地貌保持一致。

> V14 属性减半 + 死代码清理:
>
> **司机属性 4 → 2**:删除路感(road)和心力(mind),只保留 **驾驶**(决定订单解锁)和 **服务**(影响好评率)。 V13 把订单从 7 砍到 4 之后,路感和心力已经没有订单门槛挂钩,变成"养着不知道做什么"的属性,这一版彻底清掉。训练对应缩减为 2 种:模拟驾驶舱 ¥450 / 服务礼仪课 ¥500。
>
> **死代码清理**:
> - **晋升仪式整套**(rank-up CSS 动画、SFX.rankUp 音效、"✨ 张三 升任金牌司机"通知)V11 已名存实亡(单价倍率全部 1.0),这一版彻底删除 RANKS / computeRank / fareMul / salaryMul / applyTrainingRankUp / CLEAR_RANK_UP / state.rankUpEvent 全套代码。
> - **里程碑系统(ACHIEVEMENTS)**V11 已置空数组,这一版删除残留的 checkAchievements 函数、5 处调用、state.achievements 字段、ENDINGS 引用 achievements.length。
>
> **完单单价 / 日工资计算简化**:
> - `computeFare` 不再乘 rank.fareMul(永远是 1.0),直接 `order.fare × 驾驶连续加成 × rand(0.95, 1.1)`。
> - 工资按月结算:每日只累计 `Math.round(d.salary / 30)` 到本月应付工资,月报触发时一次性扣款;不乘 rank.salaryMul。
>
> **闭环影响**:属性减半后训练动机更直接——驾驶 ≥35 解锁专车订单、驾驶 ≥70 解锁豪华车订单,并持续影响高端订单收益;服务影响好评率,好评每 3 个沉淀为口碑,口碑解锁片区。司机详情面板属性条从 4 项变 2 项,司机卡片右侧的"top 2 属性"展示自动变为"全部属性"。

> V13 概念压缩 + 双循环数值微调:
>
> **车型 5 → 3**:删除汉 EV、奥德赛,保留桑塔纳(¥5,000) / 凯美瑞(¥18,000) / 奔驰 E(¥60,000),3 档价格阶梯清晰。快车由片区解锁,桑塔纳也能跑;凯美瑞起能接专车,奔驰 E 能接豪华车。
>
> **订单 7 → 4**:删除拼车(eco) / 优享(long) / 特快(night),保留 特惠订单(¥45 默认) / 快车订单(¥80 片区解锁) / 专车订单(¥180 凯美瑞起 + 驾驶≥35) / 豪华车订单(¥300 奔驰 E + 高端片区 + 驾驶≥70)。
>
> **删除时段限制**:全部订单删除 hours_window,白天黑夜统一接单,简化机制。原"特快订单 mind ≥ 60 加成 1.2×"改为高端订单按 driving 平滑加成。
>
> **片区 orderMix 重设**:每个片区都包含 short / business / airport / luxury 四类订单,但出现权重不同。开局片区特惠占大头,高级片区快车/专车/豪华占比更高,逼玩家先跑口碑扩地图,再升级车型和驾驶能力。
>   - 海甸(开局):**[short]** density 0.8,唯一初始片区
>   - 熙城(口碑 90):**[short, business]** density 0.8,第一块扩张片区
>   - 风台(口碑 180):**[airport, short, business]** density 1.0,主推专车
>   - 大馨(口碑 340):**[airport, business, short]** density 1.4,主推专车/快车
>   - 昭阳(口碑 520):**[business, luxury, short]** density 1.4,主推快车/豪华
>
> **闭环逻辑**:解锁风台/昭阳/大馨后会出现更高价订单,但专车/豪华车仍需要车型 + 驾驶承接;服务通过好评率影响口碑增长速度。光攒钱、光招人、光练驾驶或光练服务都会变慢。
>
> **V14.76 更新**:已删除 `levelUpStats` 自动成长。跑单不再提升属性,只贡献收入、订单数、好评、口碑和故事进度。

> V12 经营深度补完 — 半订单池 + 流失 + 反锁(双循环咬合):
>
> **车型解锁去口碑门槛(只用钱)**:VEHICLES.unlock 字段删除。前期攒钱受半订单池/反锁机制反作用,单纯堆钱买高级车不再可行。
>
> **半订单池机制**:每 tick 按片区 density 刷出实物订单名额(海甸/熙城 0.8 单/小时、风台 1.0、昭阳/大馨 1.4,早晚高峰 ×1.3、深夜 ×0.7)。未拥有对应车型的高阶订单只算潜在需求,不进入供需和口碑结算;车队已拥有对应车型后,司机扫描已解锁片区中可接的池项,加权抢一个并消耗。tick 末未消耗的可接车型订单视为流失。
>
> **流失惩罚**:1 单可接车型订单流失 = 城市口碑 -1(GAME.LOSS_REPUTATION_PENALTY),从第一天开始直接生效。流失数 ≥ 2 时上事件日志。
>
> **片区反锁**:isZoneUnlocked 已是动态判断,口碑跌破门槛立即反锁、回升立即重解。tick 末检测状态变化推 notif/log。无软硬之分、无缓冲期。
>
> **服务系数从 0.5% → 0.8%**:rollGoodReview 公式 `0.5 + service × 0.008`,练服务对好评率提升更明显;CrewInspector 服务行下方显式展示当前好评率。
>
> **取消 tryRate 封顶**:reputation 100 后接单率不再封顶 0.95,改为 `min(0.99, 0.7 × repMul × bonus)`,口碑持续涨持续有正反馈。
>
> **目标弹层**(UnlockRoadmapModal):入口在左侧 MissionBar 的“查看目标 →”按钮。弹层拆成“任务路线 / 游戏目标”两个 tab。任务路线展示当前任务、任务提示和 16 步主线路线;专车订单、豪华车订单作为任务节点内的牵引条件展示片区/属性/车型要求。游戏目标只展示已达成结局和当前可冲击结局,更后面的结局用问号隐藏。
>
> **HUD 运力徽章**:底部状态徽章三态切换 — 运力饱和 ✓(无流失)/ 运力紧绷 ⚠(流失 ≤ 30%)/ 订单流失 ✗(流失 > 30%,带脉冲动画)。
>
> **MissionBar 流失行**:有流失发生时(todayLost > 0 且 todayRepLoss > 0)在 MissionBar 内显示"⚠ 今日流失 X 单 · 城市口碑 −X"。
>
> **闭环逻辑**:不练服务 → 口碑慢 → 片区不解锁 → 单价上不去 → 钱攒不快 → 买不起好车;闷头攒钱 → 不招司机/不扩车队 → 流失多 → 口碑掉 → 片区反锁 → 攒钱速度更慢。光攒钱与光练服务都被自动惩罚,迫使玩家"司机/车辆/口碑"三项同步推进。

> V11 理解成本大瘦身(配合 ChatGPT 产品经理诊断):
>
> **新手引导(P0)**:首次进入 5 步 spotlight 教程(开局资源 → 顶部资金/口碑 → 左侧任务 → 左侧车组 → 地图开始按钮),挖洞高亮 + 游戏教练卡说明 + "下一步";右上角加 `?` 按钮可重新打开教程。首次未开始时底部只显示“开始运营”主 CTA,倍速控件在开始后再出现,确保新手只看到一个动作。
>
> **分级体系收敛(P1)**:删除职业等级 5 档单价倍率系统(新手/熟练/金牌/夜行神龙/专车老炮)。司机分级仅保留 4 属性 + 稀有度上限两层。`RANKS` 仅留单一 stub 兼容遗留代码引用,fareMul / salaryMul 统一 1.0。原"晋升仪式"逻辑因 rank 不再变化自然短路。
>
> **里程碑系统删除(P1)**:`ACHIEVEMENTS` 改为空数组,`checkAchievements` 自然 no-op。所有进度反馈合并到主线任务 7 条,不再有"达成阈值给奖励"的并行系统。结局结算页删除"里程碑"统计项。
>
> **声誉指标合并(P1)**:UI 上只展示城市口碑;司机个人评分 / 好评数 / 投诉数变成内部字段(后台仍计算用于事件触发),CrewInspector "个人统计" 简化为累计单数 + 累计赚得两项;订单完成日志只显示"城市口碑 +1 / -2",不再写"好评 +1 · 评分 4.7"。
>
> **三套进度条收敛(P1)**:顶栏时间胶囊只保留 `第 N 日 + HH:00 + 阶段名`,删除"今日进度 X%" 文字 + 进度条;MissionBar 删除独立的 `objective-stage` 进度条,阶段目标改为单行文字标签 `阶段目标:X`。
>
> **UI 信息冗余清理(P2)**:Fleet panel 头部精简为 `X 车组 · 点击司机训练 / 配车`,删除 fleet-summary-grid 双格(车组 / 待配对);CrewInspector hero 删除 rank 显示 + bg/月薪重复(P1-1 同步处理);死代码 `event-mini` CSS 留作以后清理。
>
> **抽卡降维(P2)**:招募候选卡删除 `gacha-desc` 长背景描述 + `gacha-cap-line` 上限文本,保留 稀有度徽章 / 头像 / 名字 / 背景名 / 月薪 / 4 属性条;modal-desc 简化为 `券名 · 选 1 名加入车队`。
>
> **招募概率简化(P2)**:删除 4 阶段 × 3 券的 12 套 `probsByPhase` 表,每档券只一个固定 `probs` 数组(普通 70/30/0/0、VIP 30/50/20/0、猎头 0/30/50/20)。`computePlayerPhase` 保留为返回 0 的 stub 仅为兼容,玩家阶段不再影响抽卡。
>
> **属性 → 解锁因果暴露(P2.5)**:CrewInspector 训练行下方增加订单状态提示,从 `ORDERS.req` 动态推导属性门槛。V14.12 后服务不再卡订单,服务行重点展示好评率;驾驶行展示专车/豪华车门槛。
>
> **未做(P3 解锁线归一)**:"车型解锁 + 片区解锁"挂同一条口碑里程碑暂未合并,留下一轮处理。

> V10.16 车组信息架构:左侧车队工作台从“待处理 / 司机 / 车辆”改为“车组 / 待配对”,一个列表项直接展示司机、车辆、状态和关键属性;右侧调度台统一为“车组详情”,把司机训练、车辆可接订单、换车和空车分配司机收在同一条人车关系里。车辆不再作为和司机并列的主入口,降低左右两块大卡片割裂和信息重复。

> V10.15 顶栏时间整合:左上角“第几日/阶段/今日进度/具体时间”合并为一个运营时间胶囊,第一行直接展示“第 N 日 + HH:00”,第二行展示“当前阶段 + 今日进度百分比”,进度条只作为辅助视觉反馈;右侧调度台空态不再重复显示大号时间,改为“待启动/运营中”状态徽标。这样玩家不需要在独立日数、阶段文字、进度条和时钟之间自行拼含义。

> V10.14 城市地图重绘:区片从“老城区/住宅区/火车站/CBD/机场”这类功能区,改成北京模拟行政区“熙城区/海甸区/风台区/昭阳区/大馨区”。区片多边形重新绘制为互不重叠的版图块,区块之间保留浅色道路留白,让城市看起来像被道路分割的真实区域,而不是互相压住的矩形卡片。

> V10.13 实时反馈稳定化:自动接单/完单只在城市地图和小车动画里表现,不再让右侧司机/车辆调度台插入或移除“正在跑单”区块,也不再让左侧司机卡随接单切换“跑单中/剩余时间”文案。底部事件日志按钮和地图下方最新事件条固定高度/宽度并单行截断,避免长日志撑开或挤压运行控制区。

> V10.12 自动接单:移除“先选片区再派司机”的核心操作,玩家点击 `1×开始` 后,有车且条件达标的司机会自动从已解锁片区匹配可接订单。城市地图保留区片解锁、订单热点和小车跑单动画,作为运营可视化面板;点击片区只查看信息,不再承担派车流程。第一条任务从“派出第一单”改为“完成第一单”。

> V10.11 司机训练轻量化:右侧司机调度不再把培训做成 4 张卡片,而是把训练合并进属性列表。每个属性一行展示“属性名、进度条、当前值/上限、单次价格、+ 按钮”,点击 `+` 就花一次钱提升一次属性;达到上限或资金不足时禁用按钮。这样玩家只需要围绕属性本身操作,不再理解“属性区 + 培训卡区”两套 UI。

> V10.10 主线提速与机制减负:主线目标从 30-45 分钟收敛到约 25-35 分钟,`TICK_MS` 调整为 2500;阶段任务从 11 步压缩到 7 步,删除“第一桶金/单独招募/单独买车”等重复教学任务;随机事件改为每 7 天一次,前 10 天只出轻事件,链式复杂事件第 21 天后才进入池子,早期事件最多展示 2 个选项;训练增益提高,减少重复点击次数;第一局失败条件只保留资金破产,忠诚/口碑降级为经营风味;里程碑奖励静默入账,不再弹窗抢主线反馈。

> V10.9 反馈合并:主线只使用“任务完成”提示,成就概念统一并入“里程碑奖励”;多个里程碑同一轮解锁时只出一张汇总卡,不再排队连续弹出。删除“第一笔生意”和“回本”两个开局低价值里程碑,第一单反馈交给主线任务承载,避免开局完成第一单后连续出现多个奖励提示。

> V10.8 信息降噪:顶栏经营状态从三张三行 KPI 卡简化为两枚资源胶囊,只显示“资金 + 数值”和“口碑 + 数值”;今日流水、今日好评/投诉和总订单量不再占顶栏,订单类目标回到左侧任务板表达。左侧车队摘要从“跑单/待配车/空车/可派”四格压缩为“司机/车辆”两格,未配车司机和空闲车辆改为醒目的待处理提示,避免过程数据长期占据主视线。

> V10.7 机制简化:移除车辆改装玩法。车辆从“车型 + 改装件”回归到车型差异;专车/优享不再需要行李架或 ETC,只需要对应车型、司机属性和片区解锁。阶段任务删除“学会改装”,车辆调度台不再显示改装件安装入口,数值后台也不再提供改装件表。

> V10.6 信息架构调整:当前资金、口碑、总订单/今日订单上移到顶栏,与天数和具体时间组成全局经营状态栏;顶栏核心指标按页面中轴居中,设置按钮留在右侧;左侧车队面板顶部新增目标板,承载当前任务和阶段目标;底部 HUD 瘦身为运行控制区,运行按钮组居中,事件日志留在左侧,只保留 `1×开始 / 1×暂停`、2×/4×/8× 和结束运营。后续 UI 验收必须按正常桌面视口检查关键文案是否串行、截断或撑破容器。

> V10.5 节奏口径:一局完整主线按 1× 基准控制在 30-45 分钟,当前 `TICK_MS = 3000` 表示 1× 下 3 秒推进 1 游戏小时,30 个游戏日约 36 分钟。2×/4×/8× 只作为压缩等待的快进工具,不作为主线时长平衡基准;底部运行按钮改为 `1×开始 / 1×暂停`,明确开始就是正常速度,且按钮文案禁止换行。

> V10.4 交互优化:城市地图的区片多边形本体变成可点击对象,不再只依赖浮动“片区调度”卡;玩家可以直接点锁定区片查看解锁条件,也可以点当前无订单的已解锁区片查看“暂无订单/等待刷新”的原因。选中片区在地图上保留描边高亮,右侧调度台负责解释当前状态,让“城市版图 → 调度台”的关系更稳定。

> V10.3 视觉回调:针对 V10.2 降噪过头的问题,恢复像素经营游戏需要的硬外框、2px 主面板边界、硬阴影、地图区片描边和主操作卡片描边;保留内部信息卡的弱线,形成“外框硬、内线轻”的层级。顶部时间 chip 只显示 `06:00` 这类具体时间,避免挤压串行;司机调度台隐藏低优先级“今日节奏”区块,个人统计改成三列两行,减少 900px 高度下右侧信息被截断。

> V10.2 视觉降噪:建立“强线/界面线/弱线/隐线”四级线条层级,只让顶栏、底部 HUD、选中态和主操作按钮保留强边框;普通面板、小统计卡、内部信息卡、地图网格、锁定片区虚线全部降为弱线或隐线;CRT 扫描线默认关闭且开启后也只保留极弱纹理,避免整屏横线造成糊感和杂乱感。

> V10.1 交互微调:底部运行控制从“暂停 + 1×/2×/4×”改为“开始/暂停 + 2×/4×/8×”,把 1× 作为默认开始速度,避免暂停和正常速度并列造成理解负担。

> V10 视觉改动:去掉当前版本所有像素/展示字体引用,正文、标题、按钮、金额、数字统一使用系统中文黑体,优先保证经营界面的可读性;时钟从图标方块改为 `06:00` 这种直接时间文本,并移动到左上角与“第几日/阶段”组成同一组游戏进程信息;地图从“点单派车”改为“片区调度”,玩家先选区片,再派司机进入该片区接单;城市地图移除背景里的虚线/实线道路装饰,保留片区版图、锁定状态、小车和运营标记作为主要视觉信息;没有可派订单时不再在地图中央弹出“训练司机/改装车辆”的空提示,让地图保持安静等待刷新;已派司机的片区会收起大号可派订单卡,只保留轻量“运营中”标记和小车动画,避免遮挡地图;地图下方司机订单/等待/休息列表从主屏移除,司机状态回到左侧车队和右侧调度台承载;阶段目标从“司机数 + 车辆数”改为“可运营车组”,一个车组等于一名已配车司机,未配车司机不再充当阶段目标产能;任务完成、成就解锁、司机里程碑统一为 `game-feedback` 游戏反馈卡,普通反馈右上堆叠,重要里程碑居中强提醒;招募券和抽卡候选都改为三列横向对比,方便玩家比较档位和候选司机;右侧司机调度改成稳定控制台,核心摘要优先展示,长内容在面板内部滚动,避免为了硬塞一屏导致信息重叠;司机能力属性条给“驾驶/服务/路感/心力”固定双字标签宽度,避免属性文案被挤成竖排;左侧车队面板增加已选司机/车辆摘要,降低“左侧选人、右侧看人”的视觉跳跃;底部 HUD 任务目标改为完整两行展示,不再截断“从城市地图挑 1 个片区并派司机出车”这类文案;“下一阶段”从资金卡移到任务区作为阶段目标,资金卡只显示当前可用资金;疲劳值保留为后台派单约束,主界面不再展示原始疲劳数字,只在影响派单时显示“休息中”;竞争事件的“加薪挽留”会展示具体司机、月薪涨幅、日成本和忠诚变化,“升职管理岗”改成明确取舍:最强司机转组长不再出车,新司机继承 40% 属性,全员忠诚 +12;网约车新政事件改为真实经营取舍,主动更新会按车辆数扣钱并为车辆写入“合规已更新”,观望会短期降低接单收益;右下角事件日志入口改成明确按钮形态,带“查看”标签、箭头块和 hover/focus 状态;订单类型统一改为特惠、特快、快车、拼车、优享、专车、豪华车这组网约车品类名。
>
> V9 重大改动:城市订单池升级为城市区片地图(多边形版图 + 锁定/解锁 + 小车跑单动画);时间节奏放慢为 1× 下 3 秒推进 1 游戏小时,4× 时也不再满屏飘收益数字;顶部瘦身、底部 HUD 承载任务/资金/口碑/倍速/日志;左侧改为车队工作台(待处理/司机/车辆 Tab),支持人数和车辆变多后的浏览;司机稀有度决定属性上限,好评/投诉写入个人评分并影响口碑;事件弹窗展示当前资金和选项后余额;成就提示改为更宽、更靠下、更长停留,并支持队列展示。
>
> V8 重大改动:城市订单池变成主操作面(地图订单气泡可点击,右侧订单调度台选择司机手动派单);开局自动派单降速(先手动派出第一单,之后自动接单逐步放开);普通任务完成改为非阻塞 toast,不暂停经营节奏;移动窄屏改为车队/城市/调度底部 Tab;头像/车标从文字占位升级为像素剪影,避免重复文字;字体改为正文清晰 + 标题展示 + 数字像素三层体系;源码拆为 `src/styles/*.css` / `src/app/*.jsx`,再用 `scripts/build-html.mjs` 内嵌回 HTML。
>
> V7 重大改动:主屏三栏调度台(司机/车辆选中后在右侧常驻面板直接操作,不再用遮挡式抽屉);司机晋升仪式(克制版,完单跨档触发顶栏提示 + 卡片闪边 + 升阶音 + 忠诚/口碑奖励);司机故事线(双层结构,每司机 1 灵魂 + 5 切片池,跨周目伪随机去重);事件冷却机制(替代一次性触发,无限可玩);新增 12 个写实事件(网约车司机真实困境)+ 8 个事件链式分支。
>
> V6 改动:司机扩到 10 种 + 4 档稀有度,抽卡式招募 + 3 档招募券,终局阈值大幅上调,里程碑扩到 18 个,终局达成自动弹窗(领奖/继续),IPO 强制结束,新增"结束运营"主动结束按钮。

> 本文档定义游戏的核心循环、数值机制、终局条件。**任何 gameplay 改动必须同步更新本文档**。
> 真正的"实际数值"在 `wycwy-data.js`,本文档只描述机制和原则。

---

## 一、核心循环

```
玩家首次点击开始运营 → 司机自动从已解锁片区匹配订单 → 时间推进
   ↓
司机跑车 → 完单赚钱(扣 20% 平台抽成)
   ↓
玩家用资金:培训司机属性 / 买更高级车 / 招募新司机
   ↓
司机属性提升 → 解锁更高单价订单(快车订单 / 专车订单 / 豪华车订单)
   ↓
口碑提升 → 解锁更高片区(风台/昭阳/大馨) + 接单密度提升
   ↓
触发阶段任务 / 随机事件 / 阶段结局
```

**节奏(V10.12)**:完整主线按 1× 基准设计,目标真实时长约 25-35 分钟。当前 `TICK_MS = 2500`,即 1× 下 2.5 现实秒 = 1 游戏小时;2×/4×/8× 只用来压缩等待,不作为通关时长平衡基准。开局不再要求手动片区调度,点击开始后司机会自动接单;关键事件、故事、结局选择会暂停,避免快进吞掉决策。

---

## 二、核心实体与关系

### 实体

| 实体 | 关键字段 | 数据位置 |
|---|---|---|
| **司机 Driver** | 2 属性(车技/服务)、背景、稀有度、薪水、当前车、后台疲劳/忠诚 | runtime state |
| **车辆 Vehicle** | 车型、驾驶员、合规状态 | runtime state |
| **订单 Order** | 类型、单价、距离、时长、属性门槛 | data.js `ORDERS`(4 种) |
| **培训 Training** | 属性、价格、增益 | data.js `TRAININGS`(2 种) |
| **任务 Mission** | 标题、目标、奖励 | data.js `MISSIONS`(16 条) |
| **结局 Ending** | 阶段、触发条件 | data.js `ENDINGS`(5 个) |
| **事件 Event** | 触发时机、选项、效果 | data.js `EVENTS`(20 + 1 投资人) |

### 关系

- **司机 ↔ 车辆**:1 对 1(司机驾驶车辆)
- **司机 ↔ 培训**:多对多(培训提升属性)
- **车辆 ↔ 订单类型**:多对多(车型 eligible 决定可接什么订单)
- **司机属性 ↔ 订单类型**:门槛关系(driving / 车技解锁专车/豪华车)
- **司机服务属性 ↔ 城市口碑**:好评率 = 0.5 + service × 0.008 → 好评 → 口碑 +1 → 解锁更高级片区

---

## 三、2 属性系统(V14)

司机只有两项可培养属性,目的是把"训练 → 更强经营能力"的因果链压到最短。

| 属性 | 直接影响 | 提升路径 |
|---|---|---|
| **车技** | 决定高端订单准入:专车订单需 ≥35、豪华车订单需 ≥70;同时让专车/豪华车单价随数值平滑增加 | 模拟驾驶舱,按当前车技 ¥300-2,600/次 |
| **服务** | 不直接卡订单类型。服务影响好评率(`0.5 + service × 0.008`),好评 → 城市口碑 +1 → 解锁更高级片区 | 服务礼仪课,按当前服务 ¥350-2,900/次 |

> **V14 改动**:V13 之前还有「路感」「心力」两个属性,但 V13 把订单从 7 种砍到 4 种之后,这两项失去了订单门槛挂钩,只剩"养着不做什么"的过时数值,V14 彻底删除。

**单价不再受职业等级影响**:V11 删除 5 档单价倍率系统,V14 删除残留 RANKS 死代码。完单只走基础单价 + 车技连续加成 + 0.95-1.1 浮动。

### 稀有度属性上限(V9 / V14 调整)

司机只能通过付费培训成长,成长上限由稀有度决定:

| 稀有度 | 上限 (driving / service) | 设计目的 |
|---|---|---|
| N 普通 | 65 / 65 | 能干活、能培养,但不是终局天花板 |
| R 稀有 | 78 / 80 | 中期主力,更容易支撑专车车组 |
| SR 史诗 | 90 / 92 | 后期骨干,能冲高端单 |
| SSR 传说 | 99 / 99 | 顶级司机,天生接近终局能力 |

司机详情里的属性区统一展示忠诚、车技、服务。忠诚显示当前值 / 忠诚上限、接单意愿和离队风险,不可手动升级;车技和服务显示当前属性 / 稀有度上限、单次价格和 `+` 按钮。每行进度条下方用一句说明解释这一项数值影响什么。

### 忠诚分层(V14.75)

| 稀有度 | 初始忠诚 | 普通忠诚上限 | 离队风险线 | 离队士气影响 |
|---|---:|---:|---:|---:|
| N 普通 | 75-85 | 100 | <25 | 其他司机忠诚 -4 |
| R 稀有 | 60-75 | 95 | <30 | 其他司机忠诚 -4 |
| SR 史诗 | 50-65 | 90 | <35 | 其他司机忠诚 -6 |
| SSR 传说 | 40-55 | 85 | <40 | 其他司机忠诚 -8 |

- `allLoyalty`:普通忠诚变化。福利、补贴、聚餐、社保、制度性照顾都走这里,正向增加最多到普通忠诚上限,负向扣减不受上限限制。
- `trustLoyalty`:信任突破。只给少数强关系选择使用:重大私人危机、严重事故公司担责、司机被抢/被冤时公司站队、个人故事奖励。正向增加可到 100,后续下降仍按当前忠诚正常扣减。
- 因忠诚过低主动离队时,公司不用付解雇补偿,但剩余司机会被扣士气。多名司机同日离队时士气扣减累加但最多 -10。

### 好评、投诉与个人评分

- 好评不只是日志文案:每次好评让该司机 `goodReviews +1`,城市口碑 `+1`,个人评分小幅上升。
- 投诉让该司机 `badReviews +1`,城市口碑 `-2`,个人评分明显下降。
- 好评率公式:`min(1, 0.5 + service × 0.008)`,服务属性是好评率唯一来源。
- 司机详情面板在“服务”属性行展示当前好评率,让玩家理解"服务属性"和城市口碑为什么有价值。

> **V14 改动**:删除「晋升仪式」整套(rank-up 边框脉冲、SFX.rankUp 三频递进音效、"✨ 张三 升任金牌司机"通知、忠诚 +20 / 口碑 +3 奖励)。V11 之后这套已经名存实亡(rank.id 永远不变),V14 彻底删除 CSS / SFX / state.rankUpEvent / CLEAR_RANK_UP action / applyTrainingRankUp 函数。

---

## 四、订单系统(V13 — 4 种)

V13 把订单从 7 种压缩到 4 种,删除时段限制(原"特快订单 22-5"和"快车 7-21"全部白天黑夜统一接单)。V14.29 后订单解锁逻辑改成“片区决定订单结构权重,车型和车技决定能不能承接高端订单”。V14.37 起,片区订单列表固定按特惠 → 快车 → 专车 → 豪华车展示,不随片区频率变化排序。

| 订单 ID | 名称 | 单价 | 时长 | 距离 | 门槛 | 车型要求 |
|---|---|---|---|---|---|---|
| `short` | 特惠订单 | ¥45 | 1h | 4km | 无 | 任意车型(桑塔纳起) |
| `business` | 快车订单 | ¥80 | 1h | 8km | 片区解锁 | 任意车型(桑塔纳起) |
| `airport` | 专车订单 | ¥180 | 2h | 20km | 车技 ≥ 35,车技越高单价越高(最高 +25%) | 凯美瑞 / 奔驰 E |
| `luxury` | 豪华车订单 | ¥300 | 2h | 12km | 高端片区 + 车技 ≥ 70,车技越高单价越高(最高 +20%) | 仅奔驰 E |

**车型 eligible**:桑塔纳能接特惠/快车,凯美瑞增加专车,奔驰 E 增加豪华车。买高级车没有"功能浪费"惩罚。

订单生成算法:
- 城市订单池根据已解锁区片、片区 density、片区 orderMix、时段倍率(早晚高峰 ×1.3、深夜 ×0.7)生成名额
- 未解锁区片会显示在地图上,但不会进入自动接单池;达到口碑门槛后自动解锁
- 已解锁区片中,当前车队没有任何可接车型的订单只作为潜在需求记录,不进入供需、流失和口碑扣分
- 司机空闲、有车、属性达标、车型可接时,系统自动从已解锁片区里加权挑选订单并出车
- 常规自动接单按 `tryRate = min(0.99, 0.7 × repMul × bgBonus)` 概率接单(V12 取消 0.95 封顶)
- repMul = 0.5 + reputation / 150(口碑越高越多单)
- 每 3 个好评沉淀为城市口碑 +1;差评仍会直接扣口碑,用于形成经营风险
- 加权挑选可接订单(rate 字段为权重)
- tick 末未消耗的可接车型名额视为流失,1 单流失 = 城市口碑 −1

---

## 五、阶段任务链(11 条主线 + 5 条静默里程碑 — V15.16 精简)

V15.16 任务系统精简:**16 → 11 显式主线任务**。被动等型(等口碑达阈值)和复合首单型(凯美瑞+车技自动达成)5 条改为 hidden 静默里程碑,达成时仅 toast 通知 + 现金奖励,不占任务卡槽,玩家进度从「N/16」紧凑为「N/11」。

| 序号 | 任务 ID | 标题 | 触发条件 | hidden | 教学的机制 |
|---|---|---|---|---|---|
| 1 | `m1_first_order` | 完成第一单 | totalCompleted ≥ 1 | — | 开始运营 + 自动接单 |
| 2 | `m2_first_day` | 跑完第一个运营日 | totalCompleted ≥ 12 且 day ≥ 2 | — | 让第一单和后续任务拉开时间间隔 |
| 3 | `m3_buy_third_car` | 购买第三辆车 | vehicles ≥ 3 | — | 买车入口 |
| 4 | `m4_recruit_third_driver` | 招募第三名司机 | drivers ≥ 3 | — | 招募入口 |
| 5 | `m5_third_crew` | 补齐第三个车组 | 可运营车组 ≥ 3 | — | 司机和车辆配对 |
| 6 | `m6_service_training` | 训练一次服务 | service 训练次数 ≥ 1 | — | 服务影响好评率和口碑 |
| — | `m7_reputation_90` | 口碑 90 → 解锁熙城区 | reputation ≥ 90 | ✓ 静默 | 被动等型 |
| 7 | `m8_driving_35` | 培养专车司机 | 任意司机车技 ≥ 35 | — | 车技门槛 |
| 8 | `m9_buy_camry` | 购买第一辆凯美瑞 | 拥有凯美瑞 | — | 专车车型门槛 |
| — | `m10_reputation_180` | 口碑 180 → 解锁风台区 | reputation ≥ 180 | ✓ 静默 | 被动等型 |
| — | `m11_first_airport` | 跑出第一单专车订单 | orderCounts.airport ≥ 1 | ✓ 静默 | 复合首单(自动触发) |
| 9 | `m12_five_crews` | 扩张到 5 个车组 | 可运营车组 ≥ 5 | — | 中期扩张 |
| 10 | `m13_buy_benz` | 购买第一辆奔驰 E | 拥有奔驰 E | — | 豪华车车型门槛 |
| 11 | `m14_driving_70` | 培养豪华车司机 | 任意司机车技 ≥ 70 | — | 高阶司机培养 |
| — | `m15_reputation_520` | 口碑 520 → 解锁昭阳区 | reputation ≥ 520 | ✓ 静默 | 被动等型 |
| — | `m16_first_luxury` | 跑出第一单豪华车订单 | orderCounts.luxury ≥ 1 | ✓ 静默 | 复合首单(isFinale 仪式仍保留) |

显式主线 11 条 → 每条玩家都有动作可执行(买/招/配/训),无"挂着等"卡顿感。
hidden 里程碑 5 条 → checkMission 乱序检查(不阻塞主线 idx 推进),完成时静默 toast + 奖励。

### 渐进式新手引导(V15.17 新增)

为降低新手认知负担,所有 UI 入口按 gate 渐进解锁,8 个 gate 配 mission/day 触发条件:

| Gate ID | 触发条件 | 解锁的 UI |
|---|---|---|
| `recruit_btn` | m1 完成第一单 | 「+ 招募」按钮 |
| `shop_btn` | m2 跑完第一日 | 「+ 买车」按钮 |
| `supply_chip` | m2 跑完第一日 | 顶栏供需指示器 |
| `training_actions` | m5 补齐三车组 | 司机详情车技/服务训练 +按钮 |
| `tryrate_card` | m5 补齐三车组 | 司机详情接单率拆解卡 |
| `salary_raise` | m6 训练一次服务 | 司机详情忠诚行调薪 +按钮 |
| `risk_actions` | day 60 | 司机详情底部「风险操作」折叠区 |
| `investor_chip` | day 80 | 顶栏「距 review N 天」倒计时 |

触发时弹半屏 splash 教学卡片(暗化背景 + 标题 + 描述 + 位置提示 + 「知道了」按钮),
关闭后 UI 入口永久可见。`UI_GATES` 配置见 wycwy-data.js,
`unlockUIGate` / `scanUIGates` / `isUIGateUnlocked` 见 wycwy-engine.js。

任务驱动 spotlight:当前主线任务对应的入口加 pulse 动画 + 红点(招募/买车两个常驻按钮),
让玩家"现在该点哪里"零困惑。

跳过开关:暂停菜单底部「跳过教学」按钮,localStorage 持久化(`wycwy-skip-onboarding`)。
开启后立刻 dispatch UNLOCK_ALL_GATES,开新游戏也立刻全解锁。
hydrateAutosaveState 老存档加载时也会根据当前 day/任务状态批量解锁,避免连环弹 splash。

---

## 六、五种结局(V10 — 阶段目标按可运营车组计算)

**可运营车组** = 已配车司机数。司机没有车时不能贡献产能,所以结局和底部阶段目标不再分别展示“司机数 / 车辆数”,而是统一展示车组数量。阶段 2 起会逐步要求更强的订单能力,且对应司机必须已配车。

游戏没有时间限制。**结局 = 真正的"游戏结束"难度**,不再是放着自动达成的低标准。

| Tier | 结局 | 资金 | 口碑 | 可运营车组 | 关键车组要求 | 难度 |
|---|---|---|---|---|---|---|
| 1 | **个体户** | ¥25,000 | 160 | 3 | — | ★★ |
| 2 | **区域品牌** | ¥80,000 | 400 | 5 | 至少 1 个专车车组 | ★★★ |
| 3 | **全市 Top 3** | ¥200,000 | 600 | 7 | 至少 1 个豪华车组 | ★★★★ |
| 4 | **被巨头收购** | ¥500,000 | 800 | 9 | 至少 3 个豪华车组 | ★★★★ |
| 5 | **IPO 上市** | ¥1,000,000 | 1000 | 12 | — (强制结束) | ★★★★★ |

**触发机制**:
- 达成新 tier(1-4)→ 弹窗"已解锁《X》结局",玩家可选 [领奖结束] 或 [继续运营冲击更高]
- 达成 tier 5 IPO → **强制结算结束**(顶级目标无更高)
- 顶栏右上角始终有"结束运营"金黄按钮 — 玩家可主动用当前最高 tier 收尾

---

## 七、失败条件(V15.16 — 三种死亡条件:钱归零主轴 + 车队崩溃副轴)

| 死亡条件 | deathCause | 触发 | 结局文案 |
|---|---|---|---|
| **资金枯竭破产** | `bankruptcy` | 资金 < 0 持续 5 天(默认) **或** 债务危机选择放弃经营 | "投资人撤资,公司破产" / "债务到期还不上,放弃经营并破产结算" |
| **被踢出局**(V15.16 新增) | `kicked_out` | Q3 review 撤资事件触发 → 强制扣大笔现金 → 资金 < 0 持续 5 天 | "未能达成业务目标,投资人终止合作" |
| **车队崩溃**(V15.16 新增) | `crew_collapsed` | 全员司机离队,公司无人运营 | "司机全员离队,公司无人运营" |

设计原则:
- **主轴 — 钱归零**:bankruptcy / kicked_out 都走「资金 < 0 持续 5 天」的判定,reason 文案区分触发路径
- **副轴 — 人散了**:crew_collapsed 独立判定,反映「黑色幽默经营悲剧」中"再多钱也开不了车"的极端时刻
- V15.16 之前的 `debt_default` 已合并进 `bankruptcy`(同属「钱归零」类型,玩家无需区分细分原因)

三种结局的区别只在 deathCause 标记 + 文案,使玩家能直观区分"我经营崩了 / 我没成长被淘汰 / 我把人都得罪光了"。

忠诚和口碑仍会被事件影响,但不作为隐藏 game over 条件。忠诚会影响司机接单意愿,低于对应稀有度的离队风险线后有日结离队风险;口碑影响片区解锁和订单流失压力。设计目的:玩家第一局只需要理解“资金别长期负数”这一个硬失败条件,但事件选择仍会改变经营效率。

### 投资人压力事件(资金负 1 天就触发,V14 多选化)

资金一旦跌破 0,**第二天日结后必定弹窗**(整个游戏只触发一次,直到资金回正再可重新触发):

> 标题:**投资人怒了**
> 文案:账上余额负数,投资人发来警告:再不解决就撤资。给你 24 小时拿出方案。

V14 起改为 **multi-choice 复选弹窗**(`InvestorPressureModal`)— 玩家可同时勾选多个动作组合救场,或选择“不采取措施,靠流水回正”的互斥选项放弃自救:

| 选项 | 立即效果 |
|---|---|
| 裁员止血 | 按车队规模裁撤高薪司机:早期 1 人,中后期约 30%-35% 司机数,保留至少 1 名司机;按裁员数量增加破产宽容期 |
| 卖车回血 | 按车队规模卖出高价车辆:早期 1 辆,中后期约 30%-35% 车辆数,保留至少 1 辆车;按 60% 残值立刻回收现金 |
| 借高利贷 | 本金按当前负债缺口动态生成,约为缺口 2-3 倍;车队越大、负债越深,本金越高、利息越高、还款期越短;每次借款生成独立债务明细 |
| 不采取措施,靠流水回正 | 互斥兜底,不裁员、不卖车、不借钱,赌接下来几天靠订单流水把现金转正。按钮下方会提示:若 N 天内资金没有回正,投资人撤资并游戏结束 |

### 债务系统(V15.11)

所有贷款统一进入 `debts` 明细数组,但不自动合并:

| 类型 | 来源 | 展示名 | 到期处理 |
|---|---|---|---|
| 扩张贷款 | 监管整改 Day 60 B 选项可选贷款 | 扩张贷款 | 到期单独扣款 |
| 高利贷 | 投资人压力里选择借款 | 高利贷 | 每借一次生成一笔独立债务 |
| 事件贷款 | 后续随机事件可配置的借款效果 | 事件贷款 | 按事件指定期限到期 |
| 重组债务 | 债务危机里主动选择重组 | 重组债务 | 合并全部未还债务后重新到期 |

顶栏资金区默认只露出最近到期债务,例如“5 天后 · 高利贷 ¥40,500”。鼠标悬停资金说明时展开全部债务,按到期日排序。到期日当天资金足够则扣除今日到期债务;资金不足则暂停并弹出“债务危机”:

| 选项 | 结果 |
|---|---|
| 放弃经营 | 进入破产结算,`deathCause = bankruptcy`(V15.16 合并:债务结算与现金流断裂同属「钱归零」死法,reason 文案区分触发路径) |
| 债务重组 | 全部未还债务合并为一笔“重组债务”;总待还 +5%;新期限为所有未还债务剩余天数之和,最少 14 天、最多 60 天 |

### 投资人定期 review(V15.16 新增 — 按绝对时间触发,惩罚 + 目标驱动双轨)

**设计目标**:稳健玩家不会触发"资金负"事件(2-3 辆车一直积攒,资金永远正数),所以现有的"投资人压力"对他们无效。新增按绝对游戏时间触发的定期 review,**前期惩罚**(撤资威胁) + **后期目标驱动**(冲更高 Tier 的愿景),覆盖稳健与激进两种玩法。

#### 评估周期(按游戏内 day 锚定)

| 评估点 | day | 8× 实时 | 性质 | 事件 ID |
|---|---|---|---|---|
| Q1 review | 90 | 9 分钟 | 惩罚 1:警告(无扣款) | `review_q1_warning` |
| 半年 review | 180 | 18 分钟 | 惩罚 2:施压(小额扣款) | `review_h1_punish` |
| Q3 review | 270 | 27 分钟 | 分岔(达标→愿景;不达标→撤资) | `review_q3_branch` |
| 年终 review | 360 | 36 分钟 | 目标驱动:愿景蓝图(无扣款) | `review_y1_vision` |

**为什么按 day 不按 month**:游戏一个月 = 30 天,8 倍速一个月 = 3 分钟。如果按 6/12/18/24 月评估,8× 下要 18/36/54/72 分钟,后两个超出 PRODUCT.md 设定的"想挑战 1 小时"区间。改用 day 90/180/270/360 把 4 次评估压缩到 9-36 分钟,匹配"5-10 分钟到 1 小时"目标受众。叙事上 day 90 = 3 个月 = 一个季度,Q1/半年/Q3/年终时间逻辑完全自洽。

#### KPI 线(以 day 为锚,基于 wycwy-data.js 实际数值反推)

| 评估点 | 资金 | 口碑 | 可运营车组 | 关键车型 | 通过门槛 |
|---|---|---|---|---|---|
| Q1(day 90) | ¥25,000 | 100 | 3 | — | 三选二满足 |
| 半年(day 180) | ¥60,000 | 280 | 5 | — | 三选二满足 |
| Q3(day 270) | ¥150,000 | 480 | 7 | ≥ 1 专车车组 | **四项全部满足** |
| 年终(day 360) | 无硬 KPI(仪式感事件) | — | — | — | — |

**KPI 设计原则**:
1. **比同期 Tier 门槛略低**,让玩家"够一够能过"。Q1 接近 Tier 1(¥25k/160/3),半年接近 Tier 2(¥80k/400/5),Q3 接近 Tier 3(¥200k/600/7)。
2. **三选二满足(Q1 / 半年)**:资金 / 口碑 / 车组三个指标里两个达标就算过 — 给玩家选择走哪条路(资金路 / 扩张路 / 口碑路),避免单一指标卡死。
3. **Q3 必须四项全部满足**:到这一步必须有专车订单(¥220/单),稳健玩家全是桑塔纳的玩家必死。这是机制最关键的卡点 — 强迫玩家在 day 180-270 之间扩张到中型车队 + 升级到专车。
4. **年终无硬 KPI**:仪式感事件,展示玩家这一年的增长曲线即可,玩家自由选择"继续冲 IPO"或"以当前 Tier 收尾"。

**反推依据**(基于 wycwy-data.js):稳健玩家(2 辆桑塔纳一直积攒)按每车日营收 ¥500、月净利 ¥8.5k 估算 — day 90 ≈ ¥35-50k / 2 辆;day 180 ≈ ¥80-100k / 2-3 辆;day 270 ≈ ¥130-180k / 3 辆。所以 Q1/半年的车组指标(3/5)是稳健玩家做不到的硬卡点,Q3 的"专车要求"是稳健玩家彻底做不到的死亡卡点。

#### 4 个评估事件细节

##### Q1 review(day 90,惩罚 1:警告)

- **触发**:`day === 90 && !investorBoosted`
- **检查**:对照 Q1 KPI(资金 ≥ ¥25k / 口碑 ≥ 100 / 车组 ≥ 3,**三选二即视为达标**)
- **达标分支**:不弹事件,顶栏 KPI 显示"距半年 review:90 天",玩家无感
- **不达标分支**:弹事件
  - **标题**:「老板有点意见」
  - **文案**:
    > 投资人微信你了。
    > "最近忙吗?看你这几个月数据没什么变化。"
    > 半小时后又来一条:"当初让你接这个车队,是觉得你能搞出更大的局面。我们投了钱,是想看到回报。下季度希望能看到些动静。"
  - **按钮**:[回复"好"](单选,关闭事件)
  - **副作用**:`investorMissCount += 1`,顶栏 KPI 单元变橙色

##### 半年 review(day 180,惩罚 2:施压)

- **触发**:`day === 180 && !investorBoosted`
- **检查**:对照半年 KPI(资金 ≥ ¥60k / 口碑 ≥ 280 / 车组 ≥ 5,**三选二即视为达标**)
- **达标分支**:不弹事件
- **不达标分支**:弹事件
  - **标题**:「来开个会吧」
  - **文案**:
    > 周六中午被叫去公司。会议室里坐了三个 VP,桌上摆着你过去半年的报表。
    > "我们觉得你最近的状态有点 plateau。"
    > "给你三个月,再给你一次机会。"
    > 临走时人事丢下一句:"这次会议的咨询费 ¥{N},从你账上扣了。"
    > 你笑着点头,签了字。
  - **按钮**:[签字](单选,关闭事件)
  - **副作用**:
    - 扣款 N **按车组规模分档**:
      - 1-3 辆 = ¥15,000("小公司的咨询费")
      - 4-6 辆 = ¥25,000("中型公司的咨询费")
      - 7+ 辆 = ¥40,000("大公司的咨询费")
    - `investorMissCount += 1`
    - 顶栏 KPI 单元变红色 + 闪烁
    - 若扣款后 funds < 0,触发现有"资金负"破产倒计时(理论上稳健玩家 ¥80k 左右,扣 ¥15k 后剩 ¥65k,痛但活)

##### Q3 review(day 270,分岔点)

- **触发**:`day === 270 && !investorBoosted`
- **检查**:对照 Q3 KPI(资金 ≥ ¥150k / 口碑 ≥ 480 / 车组 ≥ 7 / 至少 1 专车车组,**四项全部满足**)+ `investorMissCount` 计数

**分支 A — 达标 → 愿景驱动**

触发:`day === 270 && KPI 四项全部达标`
- **标题**:「Q3 数据看完了」
- **文案**:
  > 投资人这次没找麻烦。
  > "数据看了,过去半年扩了不少。现在你这个体量,可以考虑下一步了。"
  > "想没想过冲一下规上企业?做到 Tier 4 / Tier 5,我们投后再加一笔。"
- **按钮**:
  - [接受挑战 → +¥30,000 投后加注,激活 Tier 4-5 强目标驱动]
  - [稳着先 → 维持现状,不再触发后续 review 警告]
- **副作用**:
  - 选"接受挑战":`funds += 30000`,`investorBoosted = true`,顶栏 KPI 切换为"距 IPO 还差 X"模式
  - 选"稳着先":`investorBoosted = true`(不再触发评估),顶栏 KPI 单元消失

**分支 B — 不达标 + 累计 ≥ 2 次未达标 → 撤资**

触发:`day === 270 && KPI 未达标 && investorMissCount >= 2`
- **标题**:「PIP 了」
- **文案**:
  > HRBP 上午 9 点发邮件:"请于今日下午 3 点到 12 楼会议室,带上你的工牌。"
  > 投资人没来。只来了一封邮件:
  > "感谢您过去一年的服务。鉴于您未能达成既定的业务目标,我们决定终止合作。撤回投资款 ¥{N} 已从公司账户划扣。祝您下一段职业旅程一切顺利。"
  > 当晚你打开账户,余额 −¥{N}。
- **按钮**:[默认接受](单选,无法拒绝)
- **副作用**:
  - 强制扣款 N = `max(funds * 1.5, 100000)`(投资人按 1.5 倍现金抽走 — "投资款 + 利息 + 违约金",¥100k 兜底确保现金少的玩家也必死)
    - 校验:¥30k → 扣 ¥100k → -¥70k;¥80k → 扣 ¥120k → -¥40k;¥150k → 扣 ¥225k → -¥75k
  - `gameOverPending = 'kicked_out'`(标记后续 5 天破产倒计时归因)
  - 顶栏 KPI 单元改为"清算倒计时:5 天"
  - 5 天内若未通过外部事件回正(理论不可能) → 触发现有破产判定 → 死亡结局 deathCause = `kicked_out`

**分支 C — 不达标但 missCount < 2(中途追上的玩家)**

触发:`day === 270 && KPI 未达标 && investorMissCount < 2`
- 不弹撤资事件,改弹"半年 review"同款"施压"事件(扣款 + 警告)
- `investorMissCount += 1`

##### 年终 review(day 360,仪式感事件)

- **触发**:`day === 360 && 玩家未通关 && investorBoosted === true`(只对 Q3 触发过的玩家)
- **标题**:「年终 review」
- **文案**:
  > 年会上,投资人当着所有人念你这一年的数据。
  > "去年这个时候,账上 ¥X 万、车队 X 辆。今年——"
  > (展示玩家这一年增长曲线 / 关键里程碑)
  > "明年,IPO?"
- **按钮**:
  - [继续运营冲 IPO]
  - [接受当前结局收尾](触发当前已解锁的最高 Tier 结算)
- **副作用**:无强制扣款,纯叙事 + 选择

#### 与现有破产逻辑的衔接

**关键约定 — 不引入新的死亡判定**:

1. 半年 review 扣款若导致现金为负 → 触发现有 `daysFundsBelowZero` 计数 → 5 天后 `gameOver`(deathCause = bankruptcy 或 kicked_out 视上下文)
2. Q3 撤资扣款大概率立刻造成现金为负 → 同样进入 5 天破产判定 → 进入「被踢出局」结局
3. 撤资事件触发后 `gameOverPending = 'kicked_out'` 优先于 `bankruptcy`,确保 deathCause 标记正确

#### State 字段(V15.16 新增)

```javascript
reviewCounter: 0,           // 已触发评估次数(0-4),仅用于调试 / 月报回顾
investorMissCount: 0,       // 累计未达标次数,Q3 撤资判定使用(>= 2 触发撤资)
lastReviewDay: 0,           // 最近一次评估的 day,防止重复触发
investorBoosted: false,     // Q3 是否选择了"接受挑战"加注 / 稳着先 / 撤资,任一选项触发后置 true
gameOverPending: null,      // 'kicked_out' | null,撤资事件触发后标记,死亡时归因使用
```

#### 顶栏 KPI 可见性(详见第十二章 UI 规范)

顶栏新增「投资人 KPI」单元(在 funds 单元右侧):

| 阶段 | 显示 | 视觉 |
|---|---|---|
| 默认(开局-day 89) | "距 Q1 review:N 天" | 油墨次棕,无装饰 |
| Q1 警告后(day 90-179,未达标) | "距半年 review:N 天 ⚠" | 工坊橙边框 |
| 半年警告后(day 180-269,未达标) | "距 Q3 review:N 天 ⚠⚠" | 警告红边框 + 闪烁 |
| Q3 达标 + 接受挑战 | "距 IPO:¥X 万" | 金币金 + 油渍橙数字 |
| Q3 撤资 / 选稳着先 | 单元消失 | — |
| Q3 后未撤资(boosted) | "距年终 review:N 天" | 金币金 |

鼠标悬停展开 KPI vs 目标线对照表(资金/口碑/车组分别差多少),保持 PRODUCT.md 暖黄底 + 硬阴影规范,不引入新视觉语言。

---

## 八、随机事件 V15.x — unlockMission 阶段化 + 4 条真分支链式

V15.x 重构(替代 V10.10 的 chainStages 顺序触发):
- **删除 chainStages 容器机制**:不再按"chainCount % 3"顺序解锁 stage,改用 `chainChoices` 真分支
- **unlockMission 阶段化**:每个事件标 `unlockMission: N`,完成第 N 个任务后才进入抽签池
- **4 条真分支链式**(`borrow`/`platform`/`rival`/`accident`):上一段选项决定下一段走哪条
- **拆段**:`rain`/`celeb`/`newyear` 三个旧 chain 拆为 9 个独立单段事件
- **删除**:`newpolicy`(已被 V15 监管整改政策事件覆盖)
- 事件总数:35 个独立 ID,事件池过滤靠 unlockMission + requireChainChoice + requireKeyDriverAlive

### 4 条真分支链式

| chain | 角色 | 段数 | 关键机制 |
|---|---|---|---|
| `borrow` 老张借钱 | 关系信任轴 | 5 | seed → close/distance → intimate/cooled |
| `platform` 平台抽成 | 长线攒钱引导 | 1(可重复) | 抽成 +5%/次封顶 40%,选自营 −¥180k 永久完结 |
| `rival` 竞品挖人 | 钥匙司机机制 | 4 | seed → pricing 4 选项盲选 → friends_join 成功/失败 |
| `accident` 司机事故 | 信任责任轴 | 4 | seed → trust/breach → loyalty |

链式事件 NPC 画像(V15.15):

| chain | NPC | 画像定位 | 素材 |
|---|---|---|---|
| `borrow` | 老张 | 老实、要面子、长期跑车的中年司机;突出家庭开支和信任关系 | `assets/npc/npc-laozhang-borrow.png` |
| `platform` | 平台经理 | 穿西装、话术稳定的商务经理;突出规则、抽成和自营压力 | `assets/npc/npc-platform-manager.png` |
| `rival` | 滴答猎头 | 拿报价单的竞品负责人;突出人才争夺和高薪挽留 | `assets/npc/npc-rival-hunter.png` |
| `accident` | 小李 | 刚出事故后紧张又愧疚的年轻司机;突出责任归属和车队信任 | `assets/npc/npc-xiaoli-accident.png` |

UI 规则:上述链式事件弹窗使用透明 PNG Q 版像素站绘。人物固定在左侧角色舞台,可以探出弹窗上沿;右侧文字牌从人物肩线右侧开始,边框不得穿过人物。普通随机事件继续使用标准事件弹窗。玩家应能一眼区分“系统性经营事件”和“连续人物剧情”。正式玩家弹窗只展示 NPC 名字、事件标题、事件描述和玩家选项;不得展示“链式事件”“xx线”、chain、unlockMission、cooldown 等系统/策划字段。

### 钥匙司机机制(rival chain 核心)

`rival_pricing` stage 是 4 选项盲选(`blindOptions: true`,玩家选前看不到具体后果):

| 玩家选 | 实际后果 |
|---|---|
| +¥1,000 | 司机走 + 全员忠诚 -10 |
| +¥2,000 | 司机走 + 全员忠诚 -5 |
| +¥3,000 | 留 + 月薪永久 +¥3k + **钥匙司机标签** + 衰减永久减半 |
| +¥4,000 | 留 + 月薪永久 +¥4k + **钥匙司机标签** + 衰减永久减半 |

钥匙司机的 buff:
- 后续所有 `allLoyalty` 负向衰减对该司机减半(`applyAllLoyaltyWithKeyHalf`)
- `rival_seed`/`rival_pricing` 等"loseBest"事件挑选最强司机时排除(挖走免疫,`pickBestExcludingKey`)
- 解锁 mission ≥ 15 的 `rival_friends_join_success`:0 成本获得 3 或 5 个 camry 车组(¥3k → 3 套,¥4k → 5 套)
- 钥匙司机一旦离队(裁员/解雇/忠诚过低),触发 `rival_friends_join_lost`(只能"知道了",机会流失)

### state 新字段

```js
chainChoices: { borrow: 'help', rival_pricing: 3000 },  // chain 选项历史
keyDriverIds: [101, 102],                               // 钥匙司机 driver.id 列表
platformChoseSelfop: false,                             // 已选自营 → platform_pressure 不再触发
```

### 数据字段

```js
{
  id: 'borrow_seed',
  unlockMission: 0,            // 完成第 N 个任务后解锁
  chain: 'borrow',             // 链式标识符
  requireChainChoice: { borrow: 'help' },  // 前置 chainChoices 条件(支持 array OR)
  requireKeyDriverAlive: true, // 钥匙司机必须在编(true)/离队(false)
  blindOptions: true,          // 盲选事件,UI 不渲染 effect chips
  skipScale: true,             // 不走 scaleEventEffect 规模缩放
  options: [
    { label: '...', choiceKey: 'help', apply: () => ({ funds: -2000, allLoyalty: 30 }) },
  ],
}
```

### 引擎层过滤逻辑(伪代码)

```js
const completed = state.completedMissionIds.length;
const keyDriverAlive = state.keyDriverIds.some(id => state.drivers.find(d => d.id === id));
EVENTS.filter(e =>
  (e.unlockMission || 0) <= completed &&
  cooldownPassed(e) &&
  satisfyChainChoice(e.requireChainChoice, state.chainChoices) &&
  matchKeyDriverFlag(e.requireKeyDriverAlive, keyDriverAlive) &&
  (e.id !== 'platform_pressure' || !state.platformChoseSelfop)
);
```

### effect 新字段(apply 返回值)

| 字段 | 作用 |
|---|---|
| `markKeyDriver` | 把 keepBest 选中的司机加入 keyDriverIds |
| `addDrivers` / `addVehicles` / `vehicleType` | rival_friends_join_success 送 N 个司机/N 辆车 |
| `platformDone` | 设置 state.platformChoseSelfop = true |

### 35 个事件按 unlockMission 分布

| unlockMission | 对应里程碑 | 该阶段事件 |
|---|---|---|
| 0 | 开局 | rain_base、newyear_base、borrow_seed |
| 3 | m3 三辆车 | celeb_base |
| 5 | m5 三车组 | accident_seed、complaint_harass、borrow_close、borrow_distance |
| 6 | m6 服务训练 | aging_test |
| 8 | m8 车技 35 | platform_pressure、account_freeze、social_lapse |
| 9 | m9 凯美瑞 | cheating_data、celeb_pack |
| 10 | m10 口碑 180 | accident_trust、accident_breach |
| 11 | m11 第一单专车 | night_robbery、borrow_intimate |
| 12 | m12 五车组 | family_emergency |
| 13 | m13 奔驰 E | rival_seed、celeb_paparazzi、accident_loyalty |
| 14 | m14 车技 70 | quit_temptation、rain_red_alert、rival_pricing |
| 15 | m15 口碑 520 | rival_friends_join_success、rival_friends_join_lost |

---

## 八-旧、随机事件历史规范(V10.10,已被 V15.x 替代,保留作参考)

事件机制仍然冷却复用,每个事件触发后进入 25-35 天冷却,冷却结束可再次触发。V10.10 起事件频率改为每 7 天一次;前 10 天只抽轻事件(EARLY_EVENT_IDS 白名单)。**V14 起所有事件统一压成 2 选项二元抉择**(早期 V7 还有 3 选项设计,现已全部精简),核心冲突轴是「忠诚 ↔ 资金」或「资金 ↔ 口碑」,减少决策负担。

### 动态结算规则(V14.46)

- 事件原始 `apply()` 仍写基础值,便于调参和读表。
- 弹窗预览与真实结算都会经过 `scaleEventEffect(rawEffect, state)`,保证玩家看到的是最终结算数值,但不暴露内部规模系数。
- 经营规模系数由已配车车组数、当前资金和城市口碑共同决定,范围约 `1×~15×`。早期基本保持原值,中后期成本和负面事件会随公司规模变重。
- 资金影响统一取整:小额按 ¥500 档,中额按 ¥1,000 档,大额按 ¥5,000 档。正向资金奖励也会随规模增加,但倍率弱于成本,避免事件变成后期主要赚钱方式。
- 口碑正向奖励保持原整数;口碑负向惩罚按当前口碑档位放大,最高单次扣 80。口碑达到 500 后,投诉和违规类事件会明显伤害片区解锁节奏。
- `accidentRisk` 里的事故资金/口碑损失也走同一套动态结算;“风险上升”必须能在弹窗里看到概率和可能损失。

### 完整事件清单(20 + 1 投资人)

#### 现有 8 个(V6 → V7 加链式分支)

| 事件 | 标签 | 选项数 | Chain | 链 stage 数 |
|---|---|---|---|---|
| 暴雨天 / 暴雨夜地铁停运 / 50 年一遇红色预警 | 天气 | 3 | rain | 3 |
| 平台抽成 20→25% / 25→30% / 监管点名降至 15% | 行业 | 3 | platform | 3 |
| 老张借钱 ¥2k / 儿子结婚 ¥8k / 老家盖房 ¥20k | 人事 | 3 | borrow | 3 |
| 明星打车 / 经纪公司想包车 / 八卦记者跟拍 | 运气 | 2-3 | celeb | 3 |
| 3 年车龄限制 / 司机年审 / 平台被点名整改 | 监管 | 2-3 | newpolicy | 3 |
| 春节红包 / 春运抢票 / 突发返乡限制 | 节日 | 3 | newyear | 3 |
| 竞品挖最强 / 高价挖 SR+ / 整队挖角 | 竞争 | 3 | rival | 3 |
| 剐蹭豪车 / 撞行人 / 保险拒赔 | 人事 | 3 | accident | 3 |

竞争事件原则:涉及挖人时,选项必须在弹窗里提炼具体数值。加薪挽留展示“目标司机、月薪涨幅、日成本约增加、忠诚变化”;升职管理岗展示“最强司机转组长并退出一线、新司机继承属性比例、全员忠诚变化”,避免玩家不知道选项实际改变了什么。

#### V7 新增 12 个写实事件(无 chain,单段)

| 事件 ID | 标题 | 标签 | 选项数 |
|---|---|---|---|
| `account_freeze` | 平台账号被冻结 | 监管 | 3 |
| `night_robbery` | 司机半夜被抢 | 危机 | 3 |
| `complaint_harass` | 司机被投诉骚扰女乘客 | 危机 | 3 |
| `social_lapse` | 司机社保断缴了 | 人事 | 3 |
| `aging_test` | 网约车司机年龄新规 | 监管 | 3 |
| `cheating_data` | 平台诱导刷单冲业绩 | 行业 | 3 |
| `family_emergency` | 老周父亲住院 | 人事 | 3 |
| `quit_temptation` | 司机想转行送外卖 | 人事 | 3 |

**投资人压力**(危机,multi-choice 多选弹窗)— 仅资金负数 1 天后触发,独立机制不走冷却,详见前文。

### 冷却机制

- `state.eventCooldowns: { [eventId]: dayUnlocked }`,事件触发后写入解锁日
- 默认冷却 25-35 天(每事件可在 EVENTS 配 `cooldown` 字段覆盖)
- 触发频率改为每 7 天从未冷却池抽;第 10 天前只抽轻事件,第 21 天前不抽链式复杂事件

### 链式分支机制

- 8 个核心事件配 `chain` 字段 + `chainStages` 数组(每个 chain 共 3 段:stage 0 = 事件本身,stage 1 / 2 在 chainStages 内)
- `state.eventChainCount: { [chainId]: count }` 累积触发次数
- 触发时按 `count % 3` 选 stage,达到 3 后循环回 stage 0
- 链式分支让"同一类事件第二次第三次触发时文案 + 选项不同",营造"角色长期关系"感

### 调性总原则

V7 12 个新事件遵循"写实经营"调性 — 让玩家在做决策时手抖,而不是读文案时眼眶湿。三条边界:不戏剧化、不童话化、不悲情化。每个选项都有代价,无"全是好结果"的选项。

### 风险兑现规则

- 事件选项里出现“风险上升”时,必须有可见数值效果,不能只写文案。
- 暴雨硬抢单类选项使用 `accidentRisk` 概率惩罚:未触发时只获得订单/资金收益,触发时额外扣资金、口碑或司机忠诚,并写入运营日志。
- 弹窗必须提前展示事故概率和可能损失,让玩家知道这是一次风险决策。

---

## 八-A、政策事件框架(V15 — 按游戏绝对时间触发的链式黑天鹅)

### 设计目标

借鉴滴滴 2021 年下架事件,给中后期玩家设置教学型难度墙。第一次大概率破产,但破产时复盘把伏笔翻给玩家看,玩家归因到自己的决策而非游戏不公,第二次能扛住。

详细机制设计见根目录 `监管整改机制设计-V1.md`。

### 与「八、随机事件」的区别

| 维度 | 随机事件(EVENTS) | 政策事件(POLICY_EVENTS) |
|---|---|---|
| 触发方式 | 每 7 天 + 抽签 | 按游戏绝对天数(每局必触发) |
| 持续性 | 即时结算,无后续 | 链式 4 阶段,跨数月 |
| 选项 | 2 选项二元抉择 | A/B 决策点 + 子勾选(贷款) |
| 影响范围 | 单局某次的资金/口碑/忠诚 | 永久月扣款 + 订单倍率 + 状态机 |
| UI | EventModal | EventModal + PolicyDecisionModal |
| 数据位置 | `EVENTS`(wycwy-data.js) | `POLICY_EVENTS`(wycwy-data.js) |
| 触发器 | dayTick 内 `s.day % 7 === 0` 抽签 | endOfDay 内 `policyEventTick(s)` 调度 |
| state 字段 | `eventCooldowns` / `eventChainCount` | `policyState` / `policyOngoingEffects` |

### V15 监管整改完整时间线

| 游戏天数 | 阶段 | 玩家可操作 |
|---|---|---|
| Day 30 | 第 1 次通知:行业协会合规倡议 | 仅信息提示(notice_1 弹窗) |
| Day 60 | 第 2 次通知:监管约谈(决策点) | A:启动合规专项 / B:聚焦扩张窗口期 + 可选贷款 |
| Day 90 | 监管检查 / 整改公告 | A 玩家:50% verdict_pass(完全躲过) / 50% verdict_fine(轻罚 10% R₀)<br>B 玩家:必触发 verdict_ban(罚 100% R₀ + 60 天禁运) |
| Day 150 | B 玩家整改解除 | resume 弹窗 + orderMultiplier 恢复 1 |

### 关键参数(POLICY_GOV_BAN.params)

- 一次性启动费(A):40% × R₀
- 月合规衰减曲线(A/B 共用):25% → 20% → 15% → 10%(永久)
- 招募/购车冷却(A):5 天,Day 60-90 期间生效
- A 监管结果:50% pass / 50% 10% R₀ 轻罚
- B buff(Day 60-90):订单 +25% × 单均利润 +15% ≈ ×1.4375 fare
- B 贷款:100% R₀ × 10% 年化,90 天到期一次性还本付息,作为“扩张贷款”写入债务明细
- B 整改:100% R₀ 立即罚款 + 60 天 orderMultiplier 0.2 + 强制启动合规

### 抽象框架(为后续黑天鹅扩展预留)

`POLICY_EVENTS` 数组结构通用,V1 只填一个 `gov_ban`。后续加疫情、油价、限号等只需:
1. 数据层添加新条目(`schedule` + `stages` + `params`)
2. 在 engine 的 `policyEventTick` / `applyPolicyEventClose` / `applyPolicyMonthlyCompliance` 加 if 分支处理
3. UI 端 PolicyDecisionModal 已通用,无需改

`policyOngoingEffects` 字段(`orderMultiplier` / `recruitCooldownDays` / `vehicleCooldownDays`)由派单/招募/购车等子系统读取,不关心来自哪个政策事件。

### R₀(基准月营收)

决策点(Day 60)当时通过 `estimateMonthlyRevenue(s)` 计算并锁定。此后所有百分比惩罚/支出都基于 R₀ 而非当前实时营收,避免玩家"先卖车降营收来减罚款"等异常套路。

### 复盘归因

破产时(`gameOver.deathCause === 'bankruptcy'`)若 `policyState.govBan.verdictResult === 'ban'`,UI 端可在破产复盘界面展示监管整改累计影响(`policyState.govBan.stats` 已累加 fine / compliancePaid),并提示"Day 30 行业新闻"和"Day 60 监管约谈"是早期信号 — 让玩家归因到自己的决策。

> 注:V1 复盘界面 UI 增强暂未实现,V2 阶段补齐。

---

## 九、车辆系统(V13 — 3 种车型,数值见 data.js)

V13 把车型从 5 种压缩到 3 档(经济 / 中端 / 高端),价格梯度清晰,删除汉 EV / 奥德赛(功能与凯美瑞重叠)。**V12 起删除车型解锁的口碑门槛**,只用钱买。

| 车型 ID | 名称 | 售价 | 可接订单 |
|---|---|---|---|
| `santana` | 桑塔纳 | ¥5,000 | 特惠 / 快车 |
| `camry` | 凯美瑞 | ¥18,000 | 特惠 / 快车 / 专车 |
| `benz_e` | 奔驰 E | ¥60,000 | 特惠 / 快车 / 专车 / 豪华 |

**高级车反向 eligible**:凯美瑞能接特惠/快车/专车,奔驰 E 能接所有 4 种。买高级车没有"功能浪费"惩罚。

车辆没有改装件。车型差异完全通过 `eligible` 数组表达,不再有维护费、速度倍率、服务加成等隐藏字段。

---

## 十、司机背景(V14 — 10 种 × 4 档稀有度,属性 boosts 砍到 2 项)

V14 起每个司机的 `boosts` 只有 driving 和 service 两项,原数据中的 road/mind 全部删除。V14.76 起故事奖励不再发放属性,司机属性只通过付费训练提升。

V15.5 起司机姓名继续承担稀有度和角色感识别,背景作为同气质的角色说明:

| 稀有度 | 姓名规则 | 示例 |
|---|---|---|
| **N** | 两个字普通姓名 | 张伟、李军 |
| **R** | 三个字完整姓名 | 王建国、刘志强 |
| **SR** | 前缀 + 三字姓名 | 金牌张建国、王牌刘志强 |
| **SSR** | 赛车名人 + 称号后缀 | 舒马赫车神、塞纳传奇、汉密尔顿冠军 |

| 稀有度 | 背景 | driving | service | 月薪 | 忠诚倾向 | 特点 |
|---|---|---|---|---|---|---|
| **N** | 退伍军人 | 18 | 5 | ¥4,200 | 70 | 抗夜班(特点描述,V14 后已无 mind 属性体现) |
| **N** | 北漂青年 | 8 | 14 | ¥3,600 | 50 | 便宜易跳槽 |
| **N** | 二胎奶爸 | 6 | 18 | ¥3,900 | 80 | 高忠诚 |
| **N** | 下岗大叔 | 9 | 9 | ¥3,200 | 60 | 最便宜 |
| **R** | 网红司机 | 28 | 45 | ¥6,500 | 40 | 服务高 + 接单率 ×1.3 流量加成 |
| **R** | 出租老司机 | 42 | 28 | ¥7,200 | 75 | 老 Pro,车技偏强 |
| **SR** | 平台金牌司机 | 60 | 70 | ¥11,000 | 60 | 服务高,口碑增长快 |
| **SR** | 管理型骨干 | 50 | 65 | ¥12,000 | 90 | 服务偏强 + 高忠诚 |
| **SSR** | 赛车系王牌 | 95 | 80 | ¥18,000 | 50 | 车技满级,高端订单加成稳吃 |
| **SSR** | 五星服务车神 | 80 | 99 | ¥20,000 | 60 | 服务满级 + 接单率 ×1.5,口碑增长王者 |

> **V14 备注**:V13 之前的「滴答前金牌起步即金牌等级」「舒马赫起步即专车老炮 + 夜行神龙双修」等描述,因 V11 已删除职业等级体系,V14 删除晋升仪式,这些描述只保留**特点示意**,不再对应任何代码效果。

## 十-A、抽卡招募 + 招募券(V6 新增)

不再"列出固定 5 个直接选",改成**抽卡式**:

1. 玩家选**招募券档位**:
   - **普通券 ¥500** — 主出新手/熟手,起步补人
   - **VIP 券 ¥2,000** — 主出熟手/骨干,中期主力
   - **猎头券 ¥10,000** — 主出骨干/王牌,后期挖人
2. 抽出 **3 张候选卡**(随机司机背景)
3. 玩家选 1 张加入车队,或重抽(再花一次券钱),或放弃(不退钱)

候选展示(V10):
- 招募券选择页 3 张券横向排列,一眼比较价格、定位和抽卡收益。
- 抽出候选后 3 名候选横向排列,每张卡展示头像/稀有度、背景描述、属性与上限、月薪和招募按钮。
- 设计目标是三选一横向对比,让玩家像抽卡一样快速比较候选差异。
- V14.52 起候选姓名按稀有度生成,同一轮候选尽量不与当前车队和同屏候选重名。

### 稀有度概率(V11 起已简化为固定值)

每张券一个固定 [新手, 熟手, 骨干, 王牌] 4 元概率数组,不再随玩家阶段动态变化。当前数值:
- 普通招募券(¥500):新手 70% / 熟手 30%
- VIP 招募券(¥2,000):新手 30% / 熟手 50% / 骨干 20%
- 猎头券(¥10,000):熟手 30% / 骨干 50% / 王牌 20%

详见 `wycwy-data.js` 的 `RECRUIT_TICKETS.probs`。`computePlayerPhase` 阶段函数已在 V14.67 完全删除。

---

## 十一、城市分区(V10.14 — 虚构都市分区 + 解锁门槛)

城市不再只是几个热度点,而是 5 个可见区片。区片使用北京行政区名做游戏化模拟,不是严格 GIS 地图;重点是让玩家理解“城市被道路分割成多个运营区域”。锁定区片仍然画在地图上,让玩家知道下一块城市在哪里,但未解锁前不刷订单。

| 区域 | 解锁门槛 | density(单/小时) | 订单结构 orderMix | 设计目的 |
|---|---:|---:|---|---|
| 海甸区 | 0 | 0.8 | 特惠 72 / 快车 20 / 专车 6 / 豪华 2 | 唯一开局片区,基础单多但也能看到高价订单影子 |
| 熙城区 | 口碑 90 | 0.8 | 特惠 78 / 快车 16 / 专车 5 / 豪华 1 | 第一块扩张片区,继续提供稳定基础单 |
| 风台区 | 口碑 180 | 1.0 | 特惠 35 / 快车 25 / 专车 34 / 豪华 6 | 第一块专车片区,承接凯美瑞和车技训练目标 |
| 大馨区 | 口碑 340 | 1.4 | 特惠 24 / 快车 30 / 专车 38 / 豪华 8 | 中后期机场片区,提高专车和快车占比 |
| 昭阳区 | 口碑 520 | 1.4 | 特惠 18 / 快车 44 / 专车 12 / 豪华 26 | 后期高价值片区,主推快车/豪华 |

每个片区都有四类订单,差异来自权重结构。后续节奏是"用服务积累口碑 → 解锁新片区后高价订单占比提高 → 买更高级车型并训练车技承接高端订单"。

时段倍率(buildHourlySupply):早高峰(7-9)/晚高峰(17-19) ×1.3,深夜(22-5) ×0.7,其他 ×1.0。

地图表现:
- 底层使用 `assets/maps/city-map-clean-v1.png` 清新城市地图素材,上层使用 SVG 多边形作为点击热区和状态覆盖层。
- 已解锁区片只保留轻透明色块和白色边界,不遮住底图;锁定区片覆盖灰蓝半透明遮罩和虚线边界,点击后右侧调度台解释门槛。
- 跑单司机会以小车出现在对应区片周围,用位置变化 + 轻微浮动表达“正在跑单”。
- 已解锁且有可接订单的片区只显示小型“需求”标记,区片中心圆圈展示“需求 + 数值”,不再出现大号“派司机到片区”卡片。

---

## 十二、UI/视觉规范

### 一屏化

游戏全屏不滚动,所有内容固定在一个视口内。详细布局见 `src/styles/*.css`。

### 主屏调度台(V10.16)

主屏从两栏改为三栏:

| 区域 | 作用 |
|---|---|
| 顶部 | 全局经营状态栏:左侧游戏名/天数/具体时间,中间资金/口碑/订单,右侧音效/CRT 开关 |
| 左栏 | 目标板 + 车队工作台:当前任务、下一步动作、车组列表和待配对列表,负责快速选中“司机 + 车辆”关系 |
| 中栏 | 城市区片地图,展示片区解锁、订单需求和小车跑单动画 |
| 右栏 | 常驻调度台,选中片区后查看信息,选中车组后训练司机/换车/查看车辆订单范围;选中空车后分配司机 |
| 地图底部悬浮控件 | 首次开始、暂停/继续、1×/2×/4×/8× 倍速和结束运营 |

交互原则:
- 地图区片本体是信息查看入口,不是派车入口。点击片区后右侧只解释解锁条件和片区订单结构,不重复展示自动接单规则。
- 司机不再记录驻扎片区。司机每次空闲后都会重新从全部已解锁片区随机/加权找单,地图显示正在跑单的小车、预计收入和片区需求。
- 没有需求订单时,城市地图不显示中置空状态提示;这通常是开局司机/车辆少、时段刷新或片区供给造成的自然节奏,不应该误导玩家去做早期未必可行的操作。
- 顶栏承载玩家需要持续扫视的全局状态:当前资金、城市口碑、总订单和今日订单必须和天数/时间同屏出现,不能再放到底部控制区里。
- 顶栏核心指标按页面中轴居中,而不是贴右角;右上角只保留“设置 ESC”入口,音效/CRT/新手指引/诊断导出收进 ESC 系统菜单。
- 左侧目标板只承载“现在做什么”:任务编号、任务主标题和一句副标题放在车队面板顶部;主线进度不再用独立进度点重复表达,长期结局条件收进“查看路线”弹层解释,避免任务卡里出现孤立 KPI。
- 底部运行控制按游戏状态渐进展示:首次未开始只显示“开始运营”主按钮,按钮内标注“1× 正常速度”;开始后才显示 1×/2×/4×/8× 倍速。暂停后,玩家可以点击“继续运营”,也可以直接点任意倍速继续。
- 底部运行按钮组按地图中轴居中,让玩家一眼看到“开始/暂停”和倍速控制;事件日志迁移到右侧调度台日志 tab。
- 地图下方不再展示司机订单、等待、休息列表;这类信息没有直接操作价值,司机状态统一在左侧车组列表和右侧车组详情里查看。
- 不再用右侧遮挡式抽屉承载司机/车辆主操作,避免打断城市运营画面。
- 车队主对象是“车组”,不是互相独立的司机列表和车辆列表。左侧车组卡直接展示司机、车辆、状态和关键属性;未配车司机和空车进入“待配对”列表处理。
- 买车/招募会自动补齐现有空缺:买车时若有未配车司机,新车直接给他;招募时若有空车,新司机直接开车。自动配对只处理空缺,不改变已有车组。
- 右侧司机车组详情按“车组概览 → 常用操作 → 能力训练 → 风险操作”的顺序展示。顶部人车关系必须和左侧车组卡同构:司机头像 + 姓名/背景放在一起,车辆 icon + 一行完整可接订单短名放在一起;常态“等待接单 / 正在接单”不展示,只在休息、未配车、空车等异常状态下给右侧状态提示。单独选中空车时才展示“分配司机”和“车辆与订单”详情。
- 司机跑单中不可换车;车辆正在跑单时不可换驾驶员,避免状态混乱。
- 疲劳值只作为后台接单约束:完单后上涨,达到阈值后司机进入休息中并暂不可接单;主界面不展示 `疲劳 x/100` 原始数值,只展示“未配车 / 休息中 / 空车”等玩家能直接理解和行动的异常状态;“等待接单 / 正在接单”主要交给城市地图小车动画和运营日志表达。
- 车组详情面板以稳定阅读和可操作性为优先:换车/分配车辆这种常用动作固定放在训练前方;训练属性独立成能力训练区;解雇和卖车放入风险操作区,不和日常动作混排。个人统计从右侧移除,避免和当前决策无关的信息挤占操作区。
- 配车成功必须写入系统日志并给顶部通知,让玩家知道"操作成功 + 什么变了"。
- 移动窄屏不堆成长页面,改用底部 `车队 / 城市 / 调度` 三 Tab 保持一屏游戏机感。
- KPI 曲线从主屏移除,主屏只保留对当前决策有用的信息。资金和口碑集中到顶栏,底部只负责运行控制和日志入口。
- 2× 及以上快进时隐藏司机卡片收益飘字和金币粒子,降低屏幕数字抖动。
- 左上角游戏进程组同时展示“第几日/阶段”和 `HH:00` + 上午/下午/夜间标签;右上角只保留一个系统菜单入口。
- 顶栏资金/口碑/订单数字必须保持单行,超长数字要通过卡片宽度、字号和省略策略控制,禁止串行。
- 底部 HUD 不再承载任务和资金卡;长期目标统一放到“查看路线”弹层,左侧目标板只保留当前任务标题和一句达成条件。
- 底部 HUD 右侧事件日志入口必须看起来像按钮,包含可点击示能符(如“查看”标签、箭头块、硬阴影、hover/focus 状态),不能只像一段静态日志文本。
- 4S 店购车弹窗固定使用三车型横向对比。三张车卡保持等高,名称/价格、可接订单、订单标签区和购买按钮按行对齐,让价格阶梯与订单覆盖差异可以一眼比较。
- 招募券选择弹窗保持三券横向对比,但券卡不能压成横条。卡片应有足够纵向高度和内部留白,并让抽卡按钮贴底对齐。
- 左侧车队列表避免重复提示。标题区不重复说明招募/买车和待处理数量;列表项按人车关系分组展示,司机头像和姓名在一起,车辆图标和车型信息在一起,状态固定在右上角。
- 链式事件弹窗必须区别于普通随机事件:左侧使用 NPC 站绘,右侧使用对话式异形卡片承载 NPC 名字、事件标题和描述。普通事件仍保留标准头部,避免所有事件都变成高戏剧化弹窗。正式玩家弹窗不得展示 chain / unlockMission / cooldown / “xx线”等调试或策划字段。

### 字体

- 当前版本统一使用:**PingFang SC / Hiragino Sans GB / Microsoft YaHei** 系统中文黑体。
- 标题、按钮、任务条、金额、数字、徽章都不再引用 `Ark Pixel` 或 `ZCOOL QingKe HuangYou`。
- 字号阶梯仍以 **14 / 18 / 24** 三档为主,但字体抗锯齿全部恢复浏览器默认清晰渲染。
- 字体文件仍保留在目录中,仅作为历史版本或后续回退资源,当前 CSS 不加载。

### 源码维护

- `网约车物语-V3.html` 是 106 行薄壳入口,通过 `http://` 协议加载拆分后的 CSS / JSX。
- UI 样式维护入口为 `src/styles/*.css`,按基础/视觉元素/布局/工具/弹窗/响应式/游戏反馈分块。
- React 组件维护入口为 `src/app/*.jsx`,按运行时/基础组件/主界面/调度台/弹窗/App 主组件分块。
- 修改源码后直接刷新本地服务页面;当前版本不再使用 `scripts/build-html.mjs` 回写 HTML。

### 配色

- 主背景:暖黄 `#F4E4BC`(开罗风)
- 卡片:奶白 `#FFF8E7`
- 主色:亮橙 `#FF8C42`
- 强调:金黄 `#FFD93D`
- 边框:深棕 `#2A2320`(硬边框)
- 阴影:**硬阴影 2-3px 偏移,无 blur**

### 反馈动效

| 时机 | 反馈 |
|---|---|
| 司机完单 | 地图车辆位置短暂金币黄 `+¥金额` 小飘字 + 右侧像素金币/粒子 + 受倍速限流的金币音效(单点反馈,V14.27 起不再有左侧卡震动/金币粒子) |
| 培训完成 | 属性条涨 + 上升音 |
| 任务完成 | 3 音和弦 + 右上紧凑收据卡片(普通任务不暂停,展示任务名、说明和奖励) |
| 资金变化 | 顶栏数字滚动(300-400ms 缓动) |
| 按钮按下 | translate(2px, 2px) 物理感 |

> **V14 移除**:晋升仪式整套(`.rank-up-flash` CSS 动画、`SFX.rankUp` 三频递进音效、"✨ 升任金牌司机"通知)和「里程碑奖励」反馈条目。前者死代码彻底删,后者 ACHIEVEMENTS 数组已清空且函数删除。
> **V14.27 移除**:左侧车队卡的金币粒子、卡片震动、绿色"接单中预计金额"文本。完单反馈只在地图上单点呈现。
> **V14.67 移除**:`SFX.achievement` 音效函数(整体不再使用)和 `.notification-achievement` CSS。

### 8-bit 音效

优先使用本地素材音频,加载失败时回退到 Web Audio API 内联合成。当前 `SFX.complete()` 使用 `assets/audio/coin-pickup.wav`,来自 OpenGameArt 的 `8-bit platformer SFX / Pickup_Coin.wav`;4× / 8× 会降低金币音量并加播放冷却,避免高倍速完单密集时音效叠加。其余提示音仍详见 `src/app/00-runtime.jsx` 的 `SFX` 对象。可全局静音(localStorage 持久化)。

---

## 十三、司机故事线(V7 新增)

每个司机有 6 段隐藏剧情,分两层。触发条件 = 该司机个人完单数(`driver.completedOrders`)。

### 双层结构

| 层 | 数量 | 触发节点 | 行为 |
|---|---|---|---|
| **A 层 灵魂故事** | 每司机 1 段(固定) | 个人完单 = 100 | 永远不变,每周目都看到——这是该司机的"DNA",老朋友的故事 |
| **B 层 切片故事池** | 每司机 5 段池 | 个人完单 = 500 / 1000 | 从池中**伪随机抽 1 段**,优先抽未看过的;全看过后纯随机 |

### 跨周目伪随机机制

- localStorage key:`wycwy_seen_stories`
- 数据格式:`{ [bgId]: number[] }`(司机背景 ID → 已看过的切片索引)
- 算法:取池总集 - 已看过 = "未看过池";非空则从未看过抽,否则纯随机
- 灵魂故事**不写**记忆(每周目都触发)
- `RESET` action **不清空** seenStories,跨周目持久化才有意义
- 调试入口:`RESET_STORY_MEMORY` action 清空记忆,给玩家"重新体验"逃生口

### 调性总原则

留白不点破,普通人的故事最能感动人。
- 80-120 字短篇,1-2 个生活细节,不直白说情绪
- 用「他没吭声把车停好,老婆在客厅烧着一沓东西」这类细节,让玩家自己读出情绪
- 三条边界:**不戏剧化、不童话化、不悲情化**

### 奖励梯度

每段故事可配多种奖励:`{ loyalty, funds, reputation, badge }`
- 灵魂故事(100 单):中等(忠诚 +20,称号,可附带口碑或资金)
- 切片故事(500/1000 单):由该段配置决定;通常 funds +¥1,500-5,000、忠诚、口碑或称号
- 称号(`badge`)永久挂在司机的 `badges` 数组上,显示在卡片(待 UI 落地)

### 数据结构

```js
BACKGROUNDS[i].stories = {
  soul: { title, text, reward },
  slices: [
    { title, text, reward },
    ...5 段
  ]
}
```

完整 60 段文案见 `wycwy-data.js`。

### 弹窗

`StoryModal` 组件 — 竖版剧情卡,司机大头像 + 标题 + 正文 + 奖励列表 + "继续运营" 按钮。
弹窗显示时游戏暂停(`tick()` 检查 `state.activeStory`),玩家点继续后通过 `STORY_SHOWN` action 发奖励 + 写记忆。

---

## 十四、迭代约定(给未来的自己)

每次改动必须:
1. **代码改 → 同步更新本文档对应章节**
2. **数值改 → 在 `wycwy-data.js` 改,同时本文档对应表格更新**
3. **加新机制 → 加到本文档对应章节,标注"V?"版本号**
4. **删除机制 → 在本文档对应章节标注"已废弃"并保留说明**

详见项目根 `CLAUDE.md`。
